# 📈 SEO Graph Dominance, Atajoy Integration & Documentation Blueprint

## 🌐 Section 1: Technical & On-Page SEO Absolute Dominance

The portfolio application must represent the global gold standard of modern Technical SEO execution. You must implement:

### 1. Unified Linked JSON-LD Schema Graph

Inject a single, deeply consolidated schema graph payload inside the server-side root metadata pipeline containing:

- **`ProfilePage` Schema:** Showcasing your engineering credentials, exact software expertise, professional timeline, and verified technical core.
- **`WebSite` Schema:** Establishing global identity, language arrays, and brand mappings.
- **`PortfolioItem` Graph Linkages:** Interconnecting showcase elements back to dedicated technical write-ups.

### 2. Search Compliance Engines

- **Server-Generated `sitemap.xml`:** Automated page crawling with precise change frequency metrics.
- **Compliant `robots.txt`:** Fully tailored traversal directives mapping cleanly to the sitemap route.
- **Semantic HTML5 Structuring:** Enforce strict semantic nesting (`<main>`, `<article>`, `<header>`). No unsemantic div-soup layout setups.

---

## 🚀 Section 2: Project Atajoy Showstopping Showcase

Construct an elite, enterprise-grade technical case study component for **Project Atajoy** (`atajoy.com`), pulling real structural facts from the `/home/sina/projects/shop-platform/.meta/` reference data:

1. **The Architecture Grid:** Document the advanced framework (Next.js App Router, Payload CMS Postgres engine, multilingual sitemap structures, and technical schema architectures).
2. **Performance Visualizations:** Build an optimized tracker component demonstrating how Atajoy leverages Payload's Local API to eliminate network hops, achieving peak web vital speeds.

---

## 📖 Section 3: The Portfolio Engineering Manual

Adhering to the hyper-precise, production-grade documentation standards established by the `ecommerce-platform-documenter` skill in the sibling repository, you must auto-generate a master manual under `docs/portfolio-internals/`:

- **Tier A (Core Engineering Manual):** A deep-dive architectural handbook explaining the RSC/RCC boundaries, font-swapping configurations, hydration isolation layers, and server action transport structures.
- **Tier B (SEO & Content Playbook):** Step-by-step technical blueprints for updating the unified JSON-LD schema, appending new case study nodes, and executing local Lighthouse validations to ensure code updates never degrade the 100/100 CWV layout score.
