# OPTIMIZATION STATE
Updated: 2026-09-23T23:00:00Z
Approval Mode: per-task
Phase: 2 — Section Rebuilds
Sub-task: 2.5c — ProcessSection mobile performance (layer stability + off-screen layer release)
In Flight: src/components/ProcessSection/* , src/data/processData.ts
Status: done-awaiting-approval
Waiting on User Approval: yes — owner phone check of 2.5c + device/browser info (and optionally a DevTools trace) for further mobile tuning; icon-replacement question open
Plan for 2.5b (owner 2026-09-23: "it's still laggy on mobile however it got better, but the main problem is when scrolling to process section (from upper or below sections) the viewport that should be fixed to section acts late, especially on mobile … when you see it mid-way it re-fixes to viewport, fix them and make it more high performance on mobile"):
  Cause (code read): ScrollTrigger `pin` sets position: fixed from JavaScript on scroll events. Phone scrolling runs on the compositor thread ahead of JS, so the section scrolls past the top, then snaps back when the pin applies a frame or more later. Desktop scroll is driven by Lenis in the same GSAP tick as ScrollTrigger, hence mostly a phone symptom. Hero and Capabilities pin with CSS sticky (compositor-side) and don't do this.
  [x] Pin → CSS sticky (D-24): wrapper calc((--stages + 2) × 100vh), container sticky top 0, ScrollTrigger progress-only; phone icon shadow 5/12/14 px; StageIcons memoised.
  [x] Probe ALL SAME vs the 2.5 baseline (6 stages × desktop/phone, hover, above/past, pinned 6300 / 5908 px, pages 21219 / 17879); touch entry/exit sample: panel at top in 217/217 frames. Phone profile 48 fps, p95 33 ms, > 50 ms 16, script 109 ms/s, long tasks 2 (was 41–46 fps, p95 50, 25, 168, 4). Gates: build ✓, lint ✓, tsc ✓, verify 9/0/21, JS 264.8 KB gz.
  [x] D-24; commit → LOG → push
Plan for 2.5 (ProcessSection; owner 2026-09-23: "this the major laggy section in the whole site, it's a disaster in performance even on a desktop pc, i like the design and how it looks, but it's not light … make the ui/ux perfect and the performance should be blazing … the image icons are too big in mobile, and they are not real app icons … if it's expensive don't change images"):
  Read: ProcessSection.tsx (237), ProcessSection.module.css (231), processData.ts (5 nodes, 98 stage PNGs 164–235 px, 19–50 KB, 3.3 MB; public/images/Process/00-*.png 10 files 300–600 KB are unreferenced).
  Cost sources (code read; to be confirmed by profile):
    P-a .bgImage: full-screen image with FIVE chained drop-shadow filters, opacity + scale transitions (0.6 s) on every stage change → the filter chain re-evaluated every frame of the fade.
    P-b .fixedImage: up to 21 icons per stage, each filter: drop-shadow(8px 20px 24px) + drop-shadow(1px), animated x/scale/rotation/opacity 1.2 s → filter + scale on the same element = re-filter/re-raster every frame per icon; `will-change` on every icon.
    P-c icons are unmounted/remounted per stage (key = node id) → fetched/decoded again on every pass; raw <img>, lazy.
    P-d description: every word a will-change layer with a 3D rotateX (40–60 layers per stage); clip-path tween on the wrapper (main-thread paint per frame).
    P-e container: background-color + color transitions repaint the pinned full-screen layer holding the 8rem stroked titles.
    P-f resize handler: setState + ScrollTrigger.refresh() on EVERY resize — on phones the URL bar hiding/showing is a resize → re-measure + re-render while scrolling.
  Icons: stylised 3D renders (not real logos); replacing = new icon system → not now (owner: only if cheap); proposed separately.
  [x] Baseline (scratch proc-perf.mjs, traced): desktop wheel walk 15 fps, p50 83 ms, 235/365 frames > 50 ms, raster 1104 / GPU 1754 ms/s; phone touch walk (4×) 31 fps, raster 682 / GPU 1470 ms/s. process-probe (2.0 probe, reused) states + shots `.visual/20260923-2.5-before/`.
  [x] Rebuilt per D-23 (ProcessSection.tsx/.module.css, processData.ts intrinsic sizes). Gates: build ✓, lint ✓, tsc ✓, `verify-portfolio.mjs --all` → `VERIFY: 9 passed, 0 failed, 21 warning(s)`; budget / JS 264.8 KB gz, CSS 18.8 KB gz.
  [x] After: desktop 50 fps, p50 17 ms, > 50 ms 15, raster 1278–1357 / GPU 1413 ms/s; phone 46 fps, p50 17 ms, raster 359 / GPU 317 ms/s. Isolation: icon shadows = the remaining chunk (off → raster −27 %, p95 33 → 17 ms); description text-shadow and clip-path negligible → kept. Probe: all 6 stages × desktop/phone same state/colour/description/icon set, back-scroll, hover override/release, above/past pin, pin 6300 px, page 21219 — ALL SAME; 0 issues. Shots `.visual/20260923-2.5-after/`, pairs `.visual/20260923-2.5-pairs/` (desktop identical; phone icons 60 %).
Plan for 2.4b (owner 2026-09-23: "this section is too laggy and low performance on mobile … similar designs on other websites perform seamlessly … can we fix it" + "the scroll is a problem in mobile for this section, only upper half works for up/down scroll and scrolling in cards area up and down does nothing … fix this conflict and make it unified and consistent"):
  Scroll conflict (code read): the stage carries `touch-none` (touch-action: none — inherited from the WebGL version) → on phones every touch in the card half belongs to the drag; vertical swipes there never scroll. Fix: touch-action pan-y — the browser scrolls vertically (which turns the ring through the scroll spring), horizontal swipes stay a drag.
  Lag hypotheses (to measure first): (a) the full-screen background (120vh image, opacity 0.2, mix-blend-mode screen) gets a new transform every frame without its own compositor layer → repainted each frame; (b) the blend mode forces blending of that layer against the section every frame; (c) per-frame style writes on the 6 cards.
  [x] Profiled (raw CDP touch events — synthesizeScrollGesture doesn't scroll in this headless build): 1225 px touch scroll through Capabilities, 390×844 touch, 4× CPU. Before: layout 244 ms, style 525, paint 251, layerize 824, script 565, frames > 25 ms 18/332; paints every frame on #document + the sticky panel + #contact. Sources found: hero hint text-shadow loop (layout + paint every frame, page-wide), Contact background-attachment: fixed (#contact repainted every other scroll frame), Manifesto marquee + Contact blobs (main-thread style every frame while off screen), Capabilities bg moving without a layer. Touch: card-half vertical swipe moved the page 0 px.
  [x] Fixed (D-22): stage touch-pan-y; OffscreenPause leaf + [data-offscreen] rule for hero / Manifesto / Contact; Contact background → clipped fixed ::before layer; Capabilities bg + ring will-change; card style writes only on change.
  [x] After: layout 0, style 234, paint 136, layerize 597, script 369, frames > 25 ms 4/333; idle style recalc 0 (was 60/s) and 0 paints; touch — vertical swipes scroll from both halves (285 px each), sideways swipe on the cards drags (page 0 px), mostly-vertical diagonal scrolls. Contact new vs old background pixel diff inside Contact: none beyond antialiasing (desktop max 9/255 on 0.05 %; phone differences only in the Resume panel above and 1 edge row). Cylinder geometry vs WebGL: 0.00 px, opacity 0.000, 0 visibility mismatches (desktop + phone, scroll + drag). Gates: build ✓, lint ✓, tsc ✓, verify 8/1 known img/21; census 16 (+OffscreenPause.tsx); budget / JS 264.3 KB gz.
  [x] D-22 written; commit → LOG → push
Plan for 2.4 (Capabilities — CSS-3D cylinder, D-1; owner: no approval stop, push at the end):
  [x] Sources: phase-plan 2.4; cwv-invariants "CSS-3D cylinder rebuild recipe" + GSAP contract; seo-blueprint semantic contract; CapabilitiesSection.tsx (127), CylinderCard.tsx (135), src/data/capabilities.ts (6 cards), src/config/capabilities.config.ts; drei 10.7.7 web/Html.js (transform-mode maths, read in node_modules).
  Geometry (from drei Html transform mode + config): canvas = stage div (lg: 50vw × 100vh; below lg: 100% × 50vh). Camera z 35, fov 45° → CSS perspective f = P[5]·h/2 = h / (2·tan 22.5°) = 1.20711·h px on the stage, origin centre. drei maps world units 1:1 to px inside that perspective and scales each card element by distanceFactor/400 = 15/400. Equivalent CSS with k = f/35 px per world unit: ring radius 11k = 0.379378·h px, card element 320 × 420 px scaled by 0.0375·k = 0.00129333·h, card i at rotateY(i·60°) translateZ(R) (three.js and CSS rotateY share the x/z formula), ring rotateY(ρ).
  Motion/interaction inventory:
    C1 scroll: framer useScroll(section, ['start start', 'end end']) → useSpring(stiffness 100, damping 30, mass 0.8; framer rest: |Δ| < 0.005 and speed < 0.01/s → snaps to target) → ρ_scroll = p_s · (−π·6/2) = −3π over the 400vh section (sticky 100vh stage).
    C2 drag: @use-gesture useDrag axis x, cumulative offset x → ρ_drag = x·0.005 rad (no inertia; React state per move); ρ = ρ_scroll + ρ_drag. Stage has touch-action none, cursor grab / grabbing.
    C3 per frame per card: g = a_i + ρ, nz = (cos g + 1)/2, opacity nz², inner scale 0.9 + 0.1·nz; hidden when opacity < 0.05; pointer-events only when opacity > 0.9; drei z-index by camera distance.
    C4 background: back.webp, 120vh tall, translateY 0 → −15 % with p_s.
    C5 text block: whileInView (any intersection, not once) opacity 0 / x 50 ↔ opacity 1 / x 0, 0.8 s framer easeOut, reverses on leave; SSR inline opacity 0 translateX(50px).
    C6 card hover: Tailwind group-hover gradient 10 % → 20 % (CSS, 0.5 s).
  SSR today: <Canvas> renders nothing on the server → the six cards (and their text) are absent until three.js loads; the page ships three + r3f + drei + use-gesture for DOM cards.
  Design:
    - CapabilitiesSection.tsx → server shell: all markup (section#capabilities aria-labelledby, bg layer, stage with ring + 6 cards as real DOM with h3/p/ul tags, text block with h2#capabilities-title). First-paint per-card depth styles computed on the server for ρ = 0 (same formula) → the first paint shows the cylinder at rest instead of an empty stage.
    - CapabilitiesController.tsx (client leaf, renders the <section> around the server markup): ScrollTrigger (top top → bottom bottom) → spring target; spring integrated on gsap.ticker with framer's constants and rest rule (sub-stepped), ticker only while needed; ring transform, per-card styles, bg translate each frame; pointer-event drag (capture, dx·0.005, cumulative, x only); stage sizing vars (perspective, radius, card scale) from a ResizeObserver on the stage; text reveal via IntersectionObserver + GSAP (0.8 s framer easeOut, reverses).
    - CylinderCard.tsx → server component (card DOM transplanted 1:1 from the drei <Html> child).
    - Reduced motion (new): no scroll spring (rotation follows scroll directly), no bg parallax, text shown still.
    - Remove capabilities.config.ts' three.js-only fields; keep the values the CSS maths uses. Packages (three, r3f, drei, use-gesture, postprocessing) are removed in 2.8 after this ships.
  [x] Before probe (2.3c build, WebGL): card rects/opacity/visibility at settled f 0, .1, .25, .4, .5, .6, .75, .9, 1 + drag +200 px, desktop (section T 8138, 3600 px) and phone (T 5407); front card desktop 543.2 × 712.9 at x 88.4 (= 372.5 px in-plane × 1.458 perspective, confirming the maths); cards clipped at the stage (canvas) edges; text block opacity 1 / none once in view; bg translateY −15 % × p; no-JS: 0 cards in the HTML. Shots `.visual/20260923-2.4-before/{d,m}`.
  [x] Implemented: src/config/capabilities.config.ts (three.js scene values kept as the geometry source); Capability/cylinder.ts (CYLINDER per-height perspective 1.2071068 / radius 0.3793793 / card scale 0.00129333, cardAngle, maxScrollRotation, cardDepth = drei's per-frame styling); CylinderCard.tsx server component (drei <Html> card DOM 1:1, tags → ul/li, icon letter aria-hidden, first-paint depth styles for rest); CapabilitiesSection.tsx server shell (section#capabilities aria-labelledby capabilities-title, bg, stage + ring, text with SSR opacity 0 translateX(50px)); Capabilities.module.css (stage perspective + overflow hidden + vh-based fallbacks, ring preserve-3d, slot rotateY/translateZ/scale); CapabilitiesController.tsx (ResizeObserver stage vars, ScrollTrigger top top → bottom bottom, exact overdamped spring on gsap.ticker with framer's rest rule, drag = use-gesture axis x semantics read from @use-gesture/core 10.3.1 (axis on first movement, 0 px mouse/touch / 8 px pen; first 1 px discounted; cumulative offset), IntersectionObserver text reveal 0.8 s framer easeOut reversing, reduced motion). Allowlist: CapabilitiesSection.tsx + CylinderCard.tsx → CapabilitiesController.tsx.
  [x] Gates: `pnpm build` ✓; `pnpm lint` ✓; `tsc --noEmit` ✓; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 21 warning(s)` (known ProcessSection.tsx:181 img; +4 warnings = three / @react-three/fiber / @react-three/drei / @use-gesture/react now unimported → removed in 2.8); census 15. Budget / JS 9 files **264.0 KB gz** (was 503.0 — three.js stack out of the bundle) | CSS 18.5 KB gz | HTML 16.9 KB gz (+3.7: the six cards are now in the HTML).
  [x] After probe: settled card rects 0.00 px diff, opacity 0.000, visibility 0 mismatches at all 9 positions + drag, desktop and phone (first drag run was 2.7 px off before matching use-gesture's axis/step rules exactly); no-JS first paint = the WebGL rest frame (cards at the same rects; before: none). Dynamic, side by side with a 4d4c0ac worktree build (port 4311, removed after): spring rotation same curve (residual 1.1–1.8 % after aligning), CSS ring reacts ~70 ms sooner (framer measured scroll a frame later and r3f rendered in its own loop); text reveal ≤ 32 ms. Card internals identical (offsets, sizes, font metrics); full-res crop of the front card identical. Reduced motion: ring follows scroll directly, text shown, bg still. Shots `.visual/20260923-2.4-after/{d,m}`, pair sheets + card crops + no-JS shot `.visual/20260923-2.4-pairs/`.
  [x] Commit 25e08b6 `refactor(rsc)+perf(motion): 2.4 Capabilities CSS-3D cylinder (D-1)` → LOG → STATE → push
  [x] ⛔ owner review on production — 2026-09-23 "everything is fine on desktop, but in mobile we have some problems" → 2.3c approved, 2.4 approved on desktop; mobile → 2.4b
Plan for 2.3c (owner 2026-09-23: "the line going out of the point are not placed precisely (in each slide one point and line are exactly in sync but two other lines are a little off the point) … there is a small layout shift in left columns when moving from one to another (whether scrolling or clicking) fix it … then dont stop for approval, continue to 2.4 and push when done"):
  Hypotheses (from the code): (1) framer's inline `transform: scale()` replaced `.pinDotWrapper`'s `translate(-50%, -50%)`, so every dot sits +9/+9 px from the pin origin where its line starts (2.3b kept that faithfully); (2) `.projectDescContainer` keeps `padding-top: 1rem` at height 0 → a new description mounts 16 px tall and a leaving one unmounts from 16 px; the left column is vertically centred, so the titles jump ~8 px at the start and end of every change (framer had the same).
  [x] Measured on the 2.3b build: every pin's dot centre − line start = (9.0, 9.0) px (9 pins, 3 projects); FoladMarket title steps 3.2 / −3.4 / 8.0 px in single frames at 44 / 401 / 517 ms of a change (mount + unmount of the padded description).
  [x] Fixed: dot gets xPercent/yPercent −50 under the scale (first paint `translate(-50%, -50%) scale(0)`); `.projectDescContainer` keeps only overflow hidden, its `padding-top: 1rem` moves to a new inner `.projectDescInner` (settled layout identical).
  [x] Re-measured: all 9 pins (0.0, 0.0); no title step > 3 px in either direction (1→2, 2→0), positions continuous; settled states 0 diffs vs 2.3b (work-probe). Gates: build ✓, lint ✓, verify 8/1 known img/17.
Plan for 2.3b (WorkMinimal motion port):
  [x] Owner 2026-09-23 "approved, continue (and push when done to check everything on production)" → 2.3a + D-20 + D-21 approved on production 41b0a4b; push pre-authorised for 2.3b.
  [x] framer 12.38 facts (read in node_modules/motion-dom 12.38.0 + motion-utils 12.36.0): a transition with only `duration` is a keyframes tween with ease "easeOut" = cubic-bezier(0, 0, 0.58, 1) (animateMotionValue default); easeInOut = cubic-bezier(0.42, 0, 0.58, 1); `{ type: 'spring', bounce: 0.5 }` → duration-based spring, 800 ms, damping ratio 0.5 — framer's own generator sampled and fitted: closed-form underdamped spring ω0 = 15.897 rad/s, ζ = 0.5, max error 0.00097. SSR first paint = inline initial styles (bg opacity 0 / blur 20 / scale 1.05; desc opacity 0 height 0; image opacity 0 scale 0.95; pin dot scale(0) — replaces the class translate(-50%, -50%); box opacity 0 / blur 5 / translateY(±10); SVG rect + path pathLength="1" stroke-dasharray="0 1", path opacity attr 0).
  Design:
    - src/lib/motion/eases.ts: cubicBezier() (framer's exact algorithm), FRAMER_EASE_OUT / FRAMER_EASE_IN_OUT, springEase(ω0, ζ) — plain ease functions, no GSAP plugin.
    - F1 bg + F4 image: a presence-swap hook — `shown` key vs target key; target ≠ shown → exit tween from current values, then swap and enter from the initial values (framer mode="wait"); target changes during an exit only retarget the swap; target back to shown during the exit → re-enter from current.
    - F2 description: mounted while active or leaving; enter height 0→auto + opacity, exit to 0 then unmount (framer AnimatePresence, parallel).
    - F3 border: IntersectionObserver threshold 0.3 (useInView's own mechanism) → tween dasharray "v 1" to 1 / 0 over 1.5 s easeInOut from its current value.
    - F5 annotations: on mount — dot scale 0→1 with the fitted spring (0.8 s) at d, x/y pinned to 0 like framer's inline transform; path dasharray 0→1 + opacity attr 0→0.8 at d+0.1 (0.6 s easeOut); box opacity/blur/y at d+0.5 (0.4 s easeOut).
    - F6 sheet: rendered project vs requested project; enter y 100%→0 + opacity (0.4 s, cubic-bezier(0.22, 1, 0.36, 1)), exit back then unmount; D-21 focus/Escape/body lock unchanged.
    - First paint: the same inline initial styles/attributes in JSX (React never rewrites unchanged style values, GSAP owns them after mount).
    - Reduced motion (new, disclosed): every tween instant (duration 0) under prefers-reduced-motion.
    - framer-motion imports leave WorkDesktop/WorkMobile (the package itself goes in 2.8).
  [x] Before probe (41b0a4b build): frame-sampled curves (scratch curve-probe, trigger-aligned) — hover bg exit 0.8 s then enter (framer enter starts ~6 frames after the exit ends); project change: image exit 0.5 s → enter ~0.1 s later, descriptions collapse/expand in parallel (229→0 / 0→187 px, 0.4 s), annotation dot spring peaks ~1.15, line + label follow; border undraw/draw 1.5 s; phone sheet 0.4 s in/out.
  [x] Implemented: src/lib/motion/eases.ts (cubicBezier = framer's algorithm, FRAMER_EASE_OUT / IN_OUT, springEase + FRAMER_SPRING_BOUNCE_05, prefersReducedMotion); WorkDesktop — usePresenceSwap (bg + image, mode="wait" semantics incl. retarget / return-during-exit), ProjectDescription (mounted while active or leaving; derived-state `leaving` list), IntersectionObserver border (threshold 0.3, isIntersecting like framer's inView), AnnotationPoint GSAP timeline on mount (dot x/y 0 + fitted spring, line dasharray + opacity attr, label); annotations take the SHOWN project's colour (the exiting layer keeps its own, as framer's snapshot did); WorkMobile — sheetProject vs requested project, gsap.set yPercent 100 on a new sheet, tween in/out with cubicBezier(0.22,1,0.36,1), unmount after exit; D-21 focus/Escape/body lock unchanged; inline first-paint styles = framer's SSR output (checked in the built HTML). framer-motion no longer imported by WorkMinimal.
  [x] Gates: `pnpm build` ✓; `pnpm lint` ✓; `tsc --noEmit` ✓; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 17 warning(s)` (known ProcessSection.tsx:181 img); census 16. Budget / JS 503.0 KB gz (+0.6 — framer still shipped for Capabilities/Resume/Contact until 2.8; the eases + GSAP code now ride alongside).
  [x] After: curves match framer after a 1–4 frame alignment (GSAP starts 14–58 ms sooner — framer waits a React render + frame; bg enter after a hover swap 106 ms sooner); residual after alignment ≤ 2.3 % on bg/box/border/sheet, 7–8 % only on the steepest spring/height segments (one 16 ms frame ≈ 10 % there). Settled states: 0 diffs vs 2.3a-kbd (work-probe: titles, images, annotations, heights, modal flow, no-JS first paint). Reduced motion: every swap instant. Stress (hover storm, scroll storm, return to the shown project mid-exit): always one bg / image / description, labels at 1, 0 errors. Shots `.visual/20260923-2.3b-after/{d,m}` + pair sheets `.visual/20260923-2.3b-pairs/` (identical to 2.3a).
  [x] Commit b95c4ff `refactor(rsc)+perf(motion): 2.3b WorkMinimal` (D-9) → LOG → STATE → push
  [x] ⛔ owner parity review — approved 2026-09-23 ("everything approved but two small problems" → 2.3c)
Plan for 2.3a-kbd (owner 2026-09-23: "make them keyboard-accessible an push to production"; the 2.3a review itself stays open until tested on production):
  [x] Finding: globals.css shows focus rings only under body.keyboard-nav (`body:not(.keyboard-nav) *:focus { outline: none }`) and nothing ever sets that class → no focus indicator anywhere on the site today.
  Design (D-21):
    - Titles: a <button type="button" class="titleButton"> inside each title h3 (desktop + mobile). The item's existing onClick stays on the wrapper (mouse/touch target unchanged); Enter/Space on the button fires a click that bubbles to it. .titleButton = display block, inherits font/colour/stroke/alignment, no background/border/padding, cursor inherit → the same pixels. The custom cursor now shows its hover ring over titles (it reacts to buttons).
    - Desktop: aria-current="true" on the active project's button.
    - Mobile sheet: role="dialog", aria-modal, aria-labelledby its title; focus moves to the close button on open and back to the title on close; Escape closes.
    - Focus rings: new KeyboardFocus client leaf in layout.tsx sets body.keyboard-nav on Tab and clears it on any pointer down — the authored globals.css rule then shows the 2px accent outline for keyboard users only. No visible change for mouse/touch.
  [x] Implemented per Design (D-21). Gates: `pnpm build` ✓; `pnpm lint` ✓; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 17 warning(s)` (known img); client census 16 (+KeyboardFocus.tsx, allowlisted). Budget / JS 502.4 KB gz (+0.3) | CSS 18.3 KB gz.
  [x] Probe: desktop Tab → body.keyboard-nav, focused title outline 2px solid rgb(167,139,250); Enter on AutoDM → y 6968 = target, active + aria-current AutoDM; mouse click → class cleared; visible #work buttons = the 3 desktop titles only. Phone: Enter → dialog (aria-modal, labelled "SadrHub"), focus on "Close project details", body overflow hidden; Escape → closed, focus back on SadrHub, overflow ''; tap still opens, no keyboard-nav. work-probe diff vs 2.3a after-run: 0 differences (title type, rects, states, modal). Focus shot `.visual/20260923-2.3a-kbd-focus.png` (read).
  [x] D-21 written; commit → LOG → STATE; push (owner asked)
2.3 split (recorded 2026-09-23 after reading WorkMinimal.tsx (409) + .module.css (492) + workminimal-projects.ts (3 projects)): 2.3a = server shell with both layouts in the HTML and a CSS breakpoint instead of the JS `isMobile` swap, heading/semantic fixes, next/image loading props — framer-motion moves verbatim into the two client leaves; 2.3b = port the five framer animations + the spring to GSAP. Same reasoning as 2.1a/2.1b: structure verifiable to the pixel, motion reviewed on its own.
Plan for 2.3a:
  [x] Owner 2026-09-23 "approved, continue" after testing production 106887a → 2.2 Manifesto + 2.2b (D-19) approved.
  [x] Sources: phase-plan 2.3 + Phase 2 contract; cwv-invariants F-1 + GSAP contract; seo-blueprint semantic contract; WorkMinimal.tsx, WorkMinimal.module.css, workminimal-projects.ts; globals `section { position: relative }` (+ padding only with data-section) and h1–h4 rules. AGENTS.md gate: read node_modules/next/dist/docs/01-app/03-api-reference/02-components/image.md — `priority` deprecated in 16 (→ `preload`), `loading` defaults to lazy, `sizes` required with `fill`.
  Interaction inventory — WorkMinimal:
    W1 breakpoint: JS `isMobile = innerWidth <= 1024` (resize listener); SSR/first paint = desktop (isMobile false) on every device, phones swap to the mobile layout after hydration.
    W2 desktop container height (3 + 0.8) × 100vh = 380vh, sticky showcase (100vh). activeIndex = clamp(floor(−containerTop / innerHeight), 0, 2) when containerTop ≤ 0 (window scroll listener, rAF-throttled); ignored for 1 s after a click.
    W3 desktop title click → activeIndex = i, window.scrollTo({ top: containerTop + i·vh + 0.5·vh, behavior: 'smooth' }) (native smooth — same class of Lenis conflict as D-14; measured in the before probe).
    W4 desktop title hover → hoveredIndex → background shows the hovered project; inactive title fills with its colour + translateX(15px) (CSS); title stroke/colour inline from active/hovered.
    W5 mobile title tap → modal (fixed sheet) with the project; close button; body overflow hidden while open (mobile only).
    W6 annotation pin hover → dot scale 1.3 (CSS).
  Motion inventory — framer (moves verbatim in 2.3a, ported in 2.3b):
    F1 background: AnimatePresence mode="wait" keyed by displayed project; initial {opacity 0, blur 20px, scale 1.05} → {opacity 0.3, blur 0, scale 1}; exit back; 0.8 s (framer default ease for a duration-only transition — confirm in 2.3b).
    F2 active description: AnimatePresence; {opacity 0, height 0} ↔ {opacity 1, height auto}; 0.4 s.
    F3 right-column border rect: pathLength 0 → 1 while ≥ 30 % in view (not once — reverses), 1.5 s easeInOut; stroke = active project colour.
    F4 right image: AnimatePresence mode="wait" keyed by active project; {opacity 0, scale 0.95} ↔ {opacity 1, scale 1}; 0.5 s.
    F5 annotation i (delay d = 0.3 + 0.1 i): dot scale 0 → 1 spring bounce 0.5 at d; line pathLength 0 → 1 + opacity 0 → 0.8 at d + 0.1 for 0.6 s easeOut; box opacity 0 → 1, blur 5 → 0, y ±10 → 0 at d + 0.5 for 0.4 s.
    F6 mobile modal: AnimatePresence; {opacity 0, y 100 %} ↔ {opacity 1, y 0}; 0.4 s cubic-bezier(0.22, 1, 0.36, 1).
  Semantics today: project titles are <h1> (desktop list + mobile list → 4 extra h1s on the page besides the hero's); inner <section>s wrap each layout inside a div#work; annotation titles <h5> under the h1s; modal title h2, card titles h4; close button unlabeled; clickable titles are divs (no keyboard access — flagged, not changed: a <button> would switch the custom cursor to its hover state over the titles, a visible change).
  Images: desktop bg `fill sizes="100vw" priority` (container is 110 % wide — kept as is), right image `fill sizes="(max-width: 1024px) 100vw, 60vw" priority`, modal bg `sizes="100vw"`, modal image `sizes="(max-width: 1024px) 100vw, 50vw"`. The section sits ~4,500 px below the fold, so `priority` preloads two large images during page load for nothing.
  Design (2.3a):
    - WorkMinimal.tsx → server shell: <section id="work" aria-labelledby="work-title" style="--project-count: 3"> with <WorkDesktop /> + <WorkMobile />. CSS: .scrollContainer height calc((var(--project-count) + 0.8) × 100vh); at max-width 1024px height auto, .desktopShowcase display none; above it .mobileShowcase display none. JS `isMobile` removed: the desktop scroll handler checks matchMedia('(max-width: 1024px)') and stops; the mobile leaf locks body scroll only while its modal is open and the mobile query matches (live).
    - Inner <section>s → <div> (the desktop class already sets position: sticky; .mobileShowcase gets position: relative to keep the global section rule's effect).
    - Headings: desktop h2 "Selected Works" gets id="work-title" (mobile h2 unchanged, no id); project titles h1 → h3 (every class sets size/weight/margin/line-height, globals h1 and h3 share family/letter-spacing/line-height → identical computed type, verified by probe); annotation titles h5 → p (same class, no global h5 rule); modal title h2 → h3 (same computed type); close button aria-label="Close project details".
    - Images: `priority` removed from the two desktop images → default lazy (no head preload, fetched as the section approaches). `sizes` unchanged.
    - Motion: framer code moves unchanged into WorkDesktop (F1–F5) and WorkMobile (F6).
    - Visible effect: phones get the mobile list in the first paint instead of the desktop layout until hydration (the section is far below the fold, so in practice only a reload inside the section would show it).
  [x] Before probe (2.2b build = HEAD source): desktop #work DIV T 4718 H 3420 (380vh), page 21219; head preloads folad.png (640w/384w srcset); states 0/1/2 → FoladMarket/SadrHub/AutoDM (active 57.6px/900/−1.728px/63.36px, inactive 28.8px stroke 1px colour@50 %), bg + right image follow, border stroke = project colour; hover AutoDM → bg AutoDM; CLICK AutoDM from FoladMarket → y 5168 vs target 6968 (bug → D-20); page h1s: Sina Sotoudeh, FoladMarket, SadrHub, AutoDM, All About Me!; annotation titles H5 Inter 14.4px/700/0.5px/23.04px. Mobile: #work H 844 (auto), page 17879, titles H1 48px/900/−1.44px/50.4px; tap → modal (H2 40px), body overflow hidden, close unlabeled; close → gone, overflow ''. No-JS mobile: desktop layout shown, H 3207. Shots `.visual/20260923-2.3a-before/{d,m}` (0 issues).
  [x] Implemented: WorkMinimal.tsx server shell (<section id="work" aria-labelledby="work-title" style --project-count>); WorkDesktop.tsx (desktop branch + AnnotationPoint, framer verbatim; container = showcase.parentElement; scroll handler stops under WORK_MOBILE_QUERY; h1→h3, h5→p, h2#work-title; `priority` removed ×2; click → lenis.scrollTo with onComplete release, D-20); WorkMobile.tsx (mobile branch, framer verbatim; matchMedia-driven body lock; h1→h3, modal h2→h3, close aria-label); workTheme.ts (PROJECT_COLORS, projectColor, WORK_MOBILE_QUERY); CSS: container height calc((--project-count + 0.8) × 100vh), ≤1024px height auto + desktop hidden + mobile flex, mobile hidden above; .mobileShowcase position relative; allowlist WorkMinimal.tsx → WorkDesktop.tsx + WorkMobile.tsx.
  [x] Gates: `pnpm build` ✓; `pnpm lint` ✓; `tsc --noEmit` ✓; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 17 warning(s)` (known ProcessSection.tsx:181 img); client census 15, all allowlisted. Budget / JS 502.1 KB gz (−0.1) | CSS 18.2 KB gz | HTML 13.2 KB gz.
  [x] After probe diff = intended only: tags H1→H3 / H5→P / modal H2→H3 with identical computed type; page h1s → Sina Sotoudeh + All About Me! (Resume's, 2.6); aria-labelledby work-title; section tag; click lands 6968 = target; head preloads none, images loading=lazy (not fetched at load); close button labelled; no-JS mobile shows the mobile layout (H 844). Geometry, page heights, active/hover/border states, modal flow identical. Shots `.visual/20260923-2.3a-after/{d,m}` + pair sheets `.visual/20260923-2.3a-pairs/{desktop,mobile}.png` (read: identical).
  [x] Commit 759e43e `refactor(rsc): 2.3a WorkMinimal server shell and breakpoint split` (D-9) → LOG → STATE
  [ ] ⛔ owner parity review (+ D-20, keyboard question) → then 2.3b motion port
Plan for 2.2b (owner request 2026-09-23: "change every nonato mark to personal context (Sina Sotoude /sinasotoudeh for socials like linkedin,github and sina.sotoude on instagram) and push"):
  [x] Census: Manifesto.tsx:20 "At Nonato," + :33 marquee "NONATO"; Footer.tsx logo spans N·O·NATO (missed by the text grep — split letters), :119 "© … Nonato."; capabilities.ts:1 template Windows path comment. Socials: footer Twitter/LinkedIn/GitHub/Dribbble → bare home pages; mobile menu Instagram/Twitter/LinkedIn → href="#".
  Design (visible copy — owner's own request, D-19):
    - Name spelled "Sina Sotoudeh" as everywhere else on the site (resume data, nav logo, h1, metadata); the Instagram handle is taken verbatim: sina.sotoude.
    - Manifesto "At Nonato," → "For me," (keeps 26 words, so the scrub timing stays the same); marquee "NONATO" → "SINA SOTOUDEH".
    - Footer logo N[O]NATO → SINA S[O]TOUDEH (same classes, accent O echoes the nav's "SO"); copyright "© <year> Sina Sotoudeh. All rights reserved."
    - Footer socials → LinkedIn https://www.linkedin.com/in/sinasotoudeh, GitHub https://github.com/sinasotoudeh, Instagram https://www.instagram.com/sina.sotoude/ (Twitter + Dribbble removed — no profiles given; their icons go too; Instagram icon drawn as an outline glyph).
    - Mobile menu socials → Instagram, LinkedIn, GitHub with the same URLs, new tab, noopener.
    - capabilities.ts: drop the template path comment.
  [x] Edited per Design; footer logo first rendered "SINA S OTOUDEH" (the accent O's 0.1em side margins, built for N·O·NATO) → accent is now "SO" with the margins removed, reading "SINA SOTOUDEH" like the nav.
  [x] Gates: `pnpm build` ✓; `pnpm lint` ✓; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 17 warning(s)` (known ProcessSection.tsx:181 img). `grep -ri nonato src` empty; rendered page text has no "nonato" (desktop + mobile). Footer links → linkedin.com/in/sinasotoudeh, github.com/sinasotoudeh, instagram.com/sina.sotoude/ (all _blank noopener noreferrer); mobile menu Instagram / LinkedIn / GitHub; 0 console errors. Shots `.visual/20260923-2.2b-brand/` (manifesto, footer, mobile menu — read).
  [x] D-19 written; commit (D-9) → LOG → STATE; push (owner asked)
Plan for 2.2 (Manifesto):
  [x] Owner 2026-09-22 "approved, continue" after testing production 18c28c2 → 2.1b approved, D-18 stays at MAX_DPR 2, D-15 confirmed.
  [x] Sources: phase-plan Phase 2 contract + 2.2; cwv-invariants GSAP consolidation contract (useScroll+useTransform → ScrollTrigger scrub, same input range → same outputs; reduced motion via gsap.matchMedia, disclosed); seo-blueprint semantic contract; Manifesto.tsx (190) + Manifesto.module.css (150); prerendered HTML (.next/server/app/index.html); framer-motion 12.38.0; globals h1–h4 rules. AGENTS.md gate: no Next.js API touched (server component split only — pattern already read in 2.1a: 01-getting-started/05-server-and-client-components.md "Interleaving").
  Motion inventory — Manifesto:
    M1 scroll source: useScroll({ target: section, offset: ['start 80%', 'end start'] }) → progress 0 when the section top is at 80 % of the viewport, 1 when its bottom reaches the viewport top; linear, clamped.
    M2 26 words (13 + 13), word k = 1..26: range [k/26·0.5, k/26·0.5 + 0.1]; opacity 0.1→1, y 30→0 px, filter blur(12px)→blur(0px); linear, clamped (useTransform defaults).
    M3 last word "inevitable.": opacity 0.1→1, blur 12→0, y 30→0 over [0.55, 0.65]; scale 1→25 over [0.67, 1]; transform-origin center 70 %; will-change transform/opacity/filter/text-shadow (CSS).
    M4 marquee: CSS keyframes translateX 0→−50 %, 50 s linear infinite, paused on hover (stays CSS).
    M5 SSR/first paint: every word + last word inline `opacity:0.1; filter:blur(12px); transform:translateY(30px)`.
  Interaction inventory: marquee hover pause (CSS); no other handlers.
  Semantics today: no heading (eyebrow "Our Manifesto" is a span); word spans adjacent with no whitespace → text reads "Designisnotdecoration."; marquee aria-hidden.
  Design:
    - Manifesto.tsx → server component (all markup). ManifestoScrub.tsx (client leaf) renders the <section> with ref + server children (HeroController pattern); useGSAP: one timeline (duration 1) with scrollTrigger { trigger: section, start: 'top 80%', end: 'bottom top', scrub: true }; per word fromTo at position k/26·0.5, duration 0.1, ease 'none'; last word fromTo at 0.55 (0.1) + scale tween at 0.67 (0.33). Words found by DOM order (data-word / data-last-word attributes).
    - CSS: .word + .inevitable carry the SSR initial state (opacity 0.1, blur 12px, translateY 30px) so first paint is unchanged without inline styles.
    - Reduced motion (gsap.matchMedia + CSS): words and last word static and fully visible (no blur/rise/scale-up), marquee stopped. New behaviour, disclosed.
    - Semantics: section aria-labelledby="manifesto-title"; eyebrow text span → <h2 id="manifesto-title"> restating its computed look (weight 400, line-height 1.6 — verified by probe); {' '} between word spans (ignored inside the flex paragraph) → text "Design is not decoration. …".
    - "Nonato" copy stays (owner call, Phase 3).
  [x] Before probe (2.1b build = HEAD source, port 4310): geometry desktop T 3600 / H 1118, mobile T 3376 / H 1237; 7 elements × 18 progress samples (0…1 and back) + first paint; eyebrow SPAN mono 12px / 400 / 3.6px / 19.2px / uppercase / rgb(167,139,250); text "Designisnotdecoration.…"; no heading, no aria-labelledby. Shots `.visual/20260922-2.2-before/{d,m}` (p 0.1/0.3/0.5/0.62/0.8/1; 0 issues).
  [x] Implemented: Manifesto.tsx server shell (framer + directive gone; words carry data-word, last word data-last-word; spaces between word spans; eyebrow → h2#manifesto-title; section aria-labelledby); ManifestoScrub.tsx (section wrapper + useGSAP; gsap.matchMedia no-preference → one scrubbed timeline, start 'top 80%', end 'bottom top', scrub: true; word i at (i+1)/n·0.5 for 0.1; last word reveal at 0.55 for 0.1, scale 1→25 at 0.67 for 0.33; all ease none); Manifesto.module.css (first-paint state on .word/.inevitable, eyebrow weight 400 + line-height 1.6, reduced-motion block); client-allowlist Manifesto.tsx → ManifestoScrub.tsx.
  [x] Gates: `pnpm build` ✓; `pnpm lint` ✓; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 17 warning(s)` (known ProcessSection.tsx:181 img → 2.5); client census 14, all allowlisted. Budget / JS 502.2 KB gz (−0.6) | CSS 18.2 KB gz | HTML 13.5 KB gz (+0.5: the Manifesto markup now also rides in the RSC payload, as the Hero did in 2.1a).
  [x] After probe: 0 differences beyond tolerance (opacity 0.01 / blur 0.1 px / scale 0.01 / y 0.3 px) across all 18 samples × 7 elements × 2 viewports; first paint and geometry identical; eyebrow computed style + rect identical; paragraph rects identical. Only intended diffs: H2 "Our Manifesto", aria-labelledby, readable text. No-JS first paint = framer SSR (0.1 / blur 12 / translateY 30). Reduced motion: words + last word opacity 1, no filter/transform, marquee animation none. Shots `.visual/20260922-2.2-after/{d,m}`, `-2.2-after-rm/d`, pair sheets `.visual/20260922-2.2-pairs/{desktop,mobile}.png` (read: identical; marquee phase is time-based).
  [x] Commit d13d43e `refactor(rsc)+perf(motion): 2.2 Manifesto` (D-9) → LOG → STATE
  [x] ⛔ owner parity review — approved 2026-09-23 ("approved, continue", production 106887a)
Plan for 2.1b:
  [x] Owner 2026-09-22 "approved, continue" after testing production (fdb7c39) → 2.1a + 2.1c (D-16, D-17) approved. D-15 not answered explicitly → the recommended default applies (all 250 particles kept = no visible change); 2.1b measures the cost and D-15 records the result. Back-to-top: no change requested → stays as authored.
  [x] Sources: phase-plan 2.1 + Phase 2 contract (f reduced motion); cwv-invariants "Hero engine hygiene" 1–5; HeroCanvas.tsx, heroStates.ts, HeroController.tsx, Hero.module.css; existing reduced-motion rules (globals.css, Footer.module.css, CustomCursor.tsx live matchMedia pattern). AGENTS.md gate: 2.1b touches no Next.js API (DOM/canvas/CSS only) → no new docs needed.
  Motion inventory — Hero CSS (for the reduced-motion block): wordmark top/opacity (all 1.2 s expo-out, D-17 rise 50→42 %); tagline opacity 1 s + translateY 30 px 1.2 s; scroll hint neonPulse 2 s infinite alternate (opacity 0.4↔1 + glow) — overrides the hidden opacity; burst cards opacity 0.7 s / transform translateY(40px) scale(.9)→0/1 0.9 s, delays 0.1/0.3/0.6 s, float 6 s infinite (−2/−4 s), exit translateY(−30px) scale(.9) 0.5 s (≤1024 px: transform none !important); fluid items all 1.2 s, translateX(−60px)→0, stagger index×0.15 s, exit translateY(−30px) 0.5 s; fwLine width 0→60 px 1 s (+0.3 s); connect translate(−50%,−40%)→(−50%,−50%) + opacity, all 1.4 s; CTA hover scale 1.05 0.3 s.
  Engine inventory (H4) unchanged except the hygiene: 250 particles; lerp 0.06; centre lerp 0.04; rotation Date.now()·0.0003 (+×0.7 X) + mouse parallax ·0.0008; always-on rAF; buffer = CSS px; non-passive resize/mousemove.
  Design:
    - IO: IntersectionObserver on the pin section, rootMargin 200px 0px → loop runs only while the hero is within 200 px of the viewport. Rotation is absolute-time and progress is read per frame, so a resumed frame equals a never-paused one after the same ~1 s of lerp (particles sit at their fluid targets whenever the hero is left at either end).
    - Passive resize/mousemove (+ the reduced-motion scroll listener).
    - DPR: buffer = CSS size × min(devicePixelRatio, 2), ctx.setTransform(dpr…) after every buffer resize; all maths stays in CSS px. Visible: sharper particles on retina/phones (disclosed).
    - Reduced motion (live matchMedia): no loop; one static frame per hero state — the engine's own keyframes at progress 0 / 0.25 / 0.55 / 0.80 (sphere / cube / fluid / fluid, same hues), particles and centre snapped (no lerp), fixed rotation (rotY 0.6, rotX 0.4 so the cube reads as 3-D), no mouse parallax. Redrawn only when the state changes (passive scroll listener) or on resize.
    - Hero.module.css @media (prefers-reduced-motion: reduce): float + neonPulse off (the hint then simply hides outside state 0); layers cross-fade on opacity only — no slides, scales, rises or line growth.
    - Draw maths shared by both paths (one draw(progress, ease, rotY, rotX) function) — the motion path passes exactly today's values.
  [x] Before (b5c9df1 build, port 4310): canvas draws 49–60/s everywhere, incl. far below the hero; canvas frame 0.5 ms (1×) / 2.3–2.8 ms mean, p95 3.5–4.7 ms (4× CPU); script far below the hero 4× CPU: desktop 93, mobile 102 ms/s; mobile buffer 390×844. Shots `.visual/20260922-200612-2.1b-before/` + `-200634-2.1b-before-rm/` (0 issues).
  [x] Implemented: HeroCanvas.tsx — draw(progress, ease, centerEase, rotY, rotX) holds the engine maths verbatim (motion path passes 0.06 / 0.04 / clock + parallax); IntersectionObserver on the pin (200 px margin) starts/stops the loop; passive resize/mousemove/scroll; buffer CSS × min(DPR, 2) + setTransform after every resize; reduced motion (live matchMedia) → no loop, still frame per state at progress 0/0.25/0.55/0.80, snapped, rot 0.6/0.4, redrawn on state change or resize; cleanup also disconnects the observer and clears the resize timer. Hero.module.css reduced-motion block (opacity-only transitions for wordmark/tagline/cards/list/line/connect; float + neonPulse off; no rise/slide/scale transforms).
  [x] Gates: `pnpm build` ✓; `pnpm lint` ✓; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 17 warning(s)` (known ProcessSection.tsx:181 img → 2.5). Budget / JS 502.8 KB gz (+0.3) | CSS 18.1 KB gz.
  [x] After: canvas 0/s far below the hero (desktop + mobile); script far below 4× CPU desktop 93 → 55, mobile 102 → 60 ms/s; loop resumes on return (60/s at the pin end + 100 px and back at top); hero on screen unchanged at 1× (60/s, 0.5–0.6 ms). Mobile buffer 780×1688: 4× CPU frame rate 60 (1× buffer) vs 44–48 fps (2× buffer) on this SwiftShader box → D-18 (owner call). Reduced motion: 0 loop frames anywhere, 5 draws for 5 state changes. Shots `.visual/20260922-201118-2.1b-after/`, `-201142-2.1b-after-rm/`, pair sheets `.visual/20260922-2.1b-pairs/{desktop,mobile}.png` (read): motion states match before; reduced motion shows still sphere / cube / field frames, static cards, no scroll hint past state 0. D-15 written (keep 250).
  [x] Commit 12b1c99 `perf(motion): 2.1b Hero canvas hygiene and reduced motion` (D-9) → LOG → STATE
  [x] ⛔ owner parity review — approved 2026-09-22 ("approved, continue", tested on production 18c28c2); D-18 stays 2
2.1c (owner request 2026-09-22: "the mobile nav-header goes out of screen and makes every section viewport messed up" + "the big Sina Sotoudeh on hero ... goes top-left seems unnecessary because the header has the same word mark up there"):
Plan for 2.1c:
  [x] Probe (HEAD 8c0e8ab build, port 4310, mobile 390×844@3 isMobile): layout viewport 466×1009 at every scroll position, document scrollWidth 466, fixed nav 20→446 (hamburger clipped). The only unclipped offender is ResumeDashboard `.introRight` (framer initial x: 100 → right edge 466). body's existing `overflow-x: hidden` does not stop Chrome's mobile layout-viewport expansion. Live CSS test: `section#cv { overflow-x: clip }` → 390×844, scrollWidth 390, nav 20→370, 0 offenders; `html { overflow-x: clip }` also works but would turn body (overflow-x: hidden) into a scroll container and endanger the Hero's sticky pin → rejected.
  [x] Before shots `.visual/20260922-194919-2.1c-hero-before/` (top, 1000, 1800, 2500 px ×2 viewports) + `.visual/20260922-194942-2.1c-cv-before/` (#cv element).
  [x] ResumeDashboard.module.css `.root { overflow-x: clip }` (no scroll container → sticky/ScrollTrigger unaffected; overflow-y stays visible)
  [x] Hero.module.css: states 1–3 → the wordmark keeps its state-0 look and leaves with a short rise + fade (same 1.2 s transition) instead of docking top-left; the state-1/2 letter-reveal rules become unreachable and are removed; the "o" stays hidden in every state (it only ever showed while docked)
  [x] Gates: `pnpm build` ✓; `pnpm lint` ✓; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 17 warning(s)` (FAIL = known ProcessSection.tsx:181 raw img → 2.5). Budget / JS 502.5 KB gz (±0 vs 2.1a) | CSS 18.0 KB gz (−0.1).
  [x] After: overflow probe → 390×844 at all 8 positions, scrollWidth 390, nav 20→370, 0 offenders. Wordmark probe (both viewports): state 0 opacity 1; states 1–3 opacity 0, same font size, 72 px (desktop) / 68 px (mobile) higher; scrolling back to state 0 restores it. Mobile pin 3376 px (was 4036 at the inflated 1009 px vh). Shots `.visual/20260922-195147-2.1c-hero-after/`, `.visual/20260922-195213-2.1c-cv-after/`: 0 issues; #cv identical at rest; hamburger now fully on screen; the mobile back-to-top shows at the true screen corner (authored `position: fixed`, see D-16).
  [x] DECISIONS D-16 (overflow fix pulled forward from 2.6) + D-17 (wordmark exit) — owner's own requests; commit b5c9df1 (D-9) → LOG → STATE
  [x] ⛔ Owner V4 check of 2.1c — approved 2026-09-22 ("approved, continue", tested on production fdb7c39)
2.1 split (recorded 2026-09-14, after reading Hero.tsx (364) + Hero.module.css (530) + globals.css heading/element rules): 2.1a = server shell rendering all static markup + HeroController client leaf (ScrollTrigger → data-state 0..3 on the pin section, CSS keyed off it) + HeroCanvas client leaf holding the engine VERBATIM + heading/landmark semantics — zero visible change intended, proven by DOM/state/screenshot comparison; 2.1b = the canvas hygiene fixes (IntersectionObserver pause, passive listeners, DPR cap 2, reduced-motion static composition + a CSS reduced-motion block, and the mobile particle-count question as D-15). Reason: 2.1a is structural and verifiable to the pixel; 2.1b carries every visible change (sharper retina canvas, reduced-motion behaviour) and its own decision — two focused reviews instead of one entangled one, and each fits one response (the 2.0 response was cut off once by length).
Plan for 2.1a:
  [x] Sources: phase-plan 2.1 + Phase 2 contract; cwv-invariants "Hero engine hygiene" + F-3; seo-blueprint "Semantic HTML contract"; audit-baseline census row; Hero.tsx; Hero.module.css; globals.css (unlayered element rules after `@import "tailwindcss"`: h1–h4 family display / weight 700 / letter-spacing −0.03em / line-height 1.05, h1 clamp(3rem,8vw,9rem), h2 clamp(2.2rem,5vw,5.5rem), h3 clamp(1.3rem,2.5vw,2.2rem), h4 1rem / 600 / 0.05em / uppercase; `section { position: relative }`; `ul, ol { list-style: none }`; `em` italic gradient); page.tsx (Hero first in <main>); src/lib/motion/gsap.ts
  [x] AGENTS.md gate: re-read 01-getting-started/05-server-and-client-components.md "Reducing JS bundle size" + "Interleaving Server and Client Components" (server markup passed as children into a client wrapper — the HeroController shape)
  Inventory — Hero (must survive identically in 2.1a):
    H1 pin: outer <section .heroPinContainer> height 400vh; inner <section .heroPanel> position sticky, top 0, 100vh, overflow hidden
    H2 scroll state: per canvas frame, progress = clamp((scrollY − container.offsetTop) / (container.offsetHeight − innerHeight), 0, 1) (offsetTop cached at init + resize); state 0 < 0.25 ≤ 1 < 0.55 ≤ 2 < 0.80 ≤ 3; setUiState only on change; SSR/first paint = state 0
    H3 state-driven classes: wordmark `.stateN` (N = state) → `.stateN .wmGroup/.wmColor1/.wmLetterS/.wmLetterO/.wmExpand` (position/size/colour/letter reveal, transitions 0.8–1.2 s cubic-bezier(0.19,1,0.22,1)); tagline + scroll hint `.hidden` when state ≠ 0 (tagline fades + translateY 30px; the scroll hint's infinite neonPulse animation overrides `.hidden`'s opacity, so the hint keeps pulsing in states 1–3 — existing behaviour, preserved); burst cards container `.active` at 1 (cards fade/rise, delays 0.1/0.3/0.6 s, float 6 s infinite with −2/−4 s delays), `.exiting` at > 1 (0.5 s out, delay 0); fluid list `.active` at 2 (items slide in, stagger 0.15 s, line grows +0.3 s), `.exiting` at 3; connect `.active` at 3 (1.4 s rise/fade); ≤1024 px cards stack as a column (transform none !important), ≤768 px wider cards
    H4 canvas engine (moves verbatim in 2.1a; hygiene is 2.1b): 250 particles; sphere → cube (0–0.25) → fluid (0.25–0.55) → fluid; particle lerp 0.06; centre lerp 0.04 toward w/2 + 250 px (desktop, progress < 0.25) else w/2; rotation Date.now()·0.0003 (+ ×0.7 on X) + mouse parallax (pointer − half size)·0.0008 (pointer starts at 0,0); perspective focal 1000; radius clamp(base·scale·2.5, 0.3, 8); alpha clamp(scale·2, 0.1, 0.95); hue 220 → 220–270 → 290; buffer = CSS size (no DPR); resize → buffer now, new random targets after 100 ms; always-on rAF; non-passive resize/mousemove listeners
    H5 CTA <a href="#contact"> is a plain anchor outside the nav (native instant jump, hash becomes #contact); hover scale 1.05 (CSS); CustomCursor hover state via `a`
    H6 semantics today: no h1 (wordmark is div/span); nested <section> with no heading; burst titles h4 (global h4 look); fluid names h3 inside divs (`.fluidWorkItem h3` + global h3); connect headline h2; canvas not hidden from AT
  Design (2.1a):
    - Hero.tsx → server component rendering all markup. Client leaves: HeroController (renders the pin <section> with data-state={state} and the server panel as children; useGSAP + ScrollTrigger.create({ trigger: section, start: 'top top', end: 'bottom bottom', onUpdate + onRefresh → state }) — same progress formula as H2, synced to Lenis in the same frame, and it no longer rides the canvas loop) and HeroCanvas (the <canvas> + engine from H4 verbatim, container via closest(), setUiState removed). heroStates.ts holds the thresholds + stateFor(progress) shared by both.
    - CSS: every state selector keeps its specificity — `.stateN .x` → `[data-state="N"] .x`; `.heroTagline.hidden` / `.heroScrollHint.hidden` → `[data-state="1|2|3"] .heroTagline`; `.active .burstCard` → `[data-state="1"] .burstCard`; `.exiting .burstCard` → `[data-state="2|3"] .burstCard`; `.active .fluidWorkItem/.fwLine` → `[data-state="2"] …`; `.exiting .fluidWorkItem` → `[data-state="3"] …`; `.heroConnect.active` → `[data-state="3"] .heroConnect`; unused .state0–3/.hidden/.active/.exiting classes dropped.
    - Semantics: pin <section aria-labelledby="hero-title">; inner panel <section> → <div> (class keeps sticky; global `section` rule only set position: relative, overridden by the class anyway); `.wmGroup` → <h1 id="hero-title"> and its rows → <span> (both classes already set display:flex and every font property the global h1 rule sets) with a whitespace node between the rows (ignored inside a flex container, so no visual change; text becomes "Sina Sotoudeh" instead of "SinaSotoudeh"); burst titles h4 → h2 with a `.bcTitle` class restating the global h4 look (1rem / 600 / 0.05em / uppercase; display family and line-height 1.05 are shared by the global h2 and h4 rules) so the global h2 size never applies; fluid list div → <ul>, items → <li>, names h3 → <p class="fwName"> restating global h3 family/letter-spacing/line-height (size/colour/margin/weight already in the module); decorative canvas container aria-hidden. Outline: h1 Sina Sotoudeh → h2 Build/Design/Launch → h2 Let's build something inevitable. The page still has four other h1s (WorkMinimal ×3 — one per project: FoladMarket, SadrHub, AutoDM — and Resume "All About Me!") — fixed in 2.3 / 2.6, page-wide check 3.4.
    - F-3: the engine leaf is small (~3 KB) and above the fold → no next/dynamic.
    - Template copy flagged, not changed: fluid list names/tags ("Nexus Identity"… "AI Integration") look like template placeholders (owner call with the Phase 3 copy sign-off, like "Nonato").
  [x] Before capture on the production build of HEAD 9ae8464 (port 4310, own PID stopped): scratch hero-probe (class-agnostic computed styles of wordmark / letters / tagline / hint / cards / fluid items / lines / connect at 13 scroll positions per viewport — top, each threshold ±20 px, 0.40 / 0.70 / 1.00, below, back 0.40, back top — plus reload at 0.90, burst shots +150/400/900/1600 ms and fluid shots +300/800 ms after crossing, hero text + heading outline, page h1s, canvas buffer, rAF census). Desktop: pin 3600 px, canvas buffer 1440×900; states by position 0011122233310; reload → 3. Mobile (overflowing page → innerHeight 1009 in emulation): pin 3376, buffer 390×844, same state sequence. Hero text "SinaSotoudeh…"; headings H4 Build/Design/Launch, H3 ×5 names, H2 connect; page h1s FoladMarket / SadrHub / AutoDM / All About Me!. 0 errors.
  [x] Implemented per Design: Hero.tsx server shell (all markup; the undefined `styles.wmSecondWord` class — no such rule — dropped from the second row's class string, the only DOM-attribute removal); HeroController.tsx; HeroCanvas.tsx (engine verbatim except: container = canvas.closest('section'), per-frame state/setUiState removed, progress via heroProgress()); heroStates.ts (HERO_STATES, heroProgress(section, top), heroStateFor(progress)); Hero.module.css state selectors → [data-state] (same specificity; the hidden rule uses `[data-state]:not([data-state="0"])`, which only competes with the base rules), `.bcTitle`, `.fwName`; client-allowlist: −Hero.tsx +HeroCanvas.tsx +HeroController.tsx
  [x] Fix found by the probe (first after-run): with ScrollTrigger's own `end: 'bottom bottom'` the mobile thresholds moved 40–140 px later (ScrollTrigger measured the 844 px viewport; the engine always used window.innerHeight = 1009 on the overflowing emulated page), so DOM layers and particle morph could disagree. HeroController now takes only the timing from ScrollTrigger (onUpdate/onRefresh, end = section height − innerHeight) and the state from heroProgress() — the engine's own measure, shared.
  [x] Gates: `pnpm build` ✓; `pnpm lint` ✓; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 17 warning(s)` (FAIL = known ProcessSection.tsx:181 raw img → 2.5; census 14 client modules, all allowlisted). Budget: / JS 11 files 502.5 KB gz / 424.3 KB br / 1755.3 KB raw (−0.6 KB gz vs 2.0) | CSS 18.1 KB gz | HTML 13.0 KB gz (+0.9: hero markup in the RSC payload).
  [x] After-probe (rebuilt): every sample and the reload identical on both viewports (states 0011122233310 both); the only 10 diffs are the intended ones — text "Sina Sotoudeh…", headings H1 Sina Sotoudeh / H2 Build / H2 Design / H2 Launch / H2 connect, page h1s gain "Sina Sotoudeh", card titles H2 with identical computed 16px / 600 / 0.8px / uppercase / 16.8px / display family, names P with identical 56px (mobile 32px) / 400 / −1.68px / 58.8px / display family / white / margin 0. rAF census unchanged. State-switch latency (MutationObserver, 6 jumps each): after 6–18 ms desktop / 13–15 ms mobile; before (worktree build of 9ae8464 on :4311, removed after) 8–23 / 10–17 → both one frame. Timing sheets: desktop burst/fluid pairs match; the first mobile burst pairs looked offset but a CSS-scale repeat (2 runs per build) matches at 150/400/900 ms → @3x capture latency, not behaviour. Screenshot set `.visual/20260914-043347-2.1a-hero-after/` 24 + 22, heights 21219 / 17764, 0 issues; hero-range pairs vs 2.0 set `.visual/20260914-043347-2.1a-hero-pairs/` read — identical (particles time-varying).
  [x] Commit `refactor(rsc)+perf(motion): 2.1a Hero server shell and state controller` (D-9) → LOG → STATE
  [x] ⛔ Owner parity review (V4) — approved 2026-09-22 ("approved, continue")
2.0 — closed 2026-09-14 (commits 5890dfc + 9ae8464, owner-approved "approved, continye"): shared motion module, GSAP ticker drives Lenis, D-14 links via lenis.scrollTo. Evidence kept below.
Plan for 2.0:
  [x] Sources read this session: phase-plan Phase 2 contract + 2.0; cwv-invariants "The engine — GSAP consolidation contract"; DECISIONS D-2 / D-13 / D-14; LenisProvider.tsx, ProcessSection.tsx (+ .module.css wrapper/container), NavigationController.tsx, BackToTopButton.tsx, layout.tsx; node_modules: @gsap/react 2.1.2 src + types (useGSAP = gsap.context in a layout effect; `useGSAP.register`), gsap 3.14.2 package exports ("gsap" → ESM index.js; "gsap/dist/ScrollTrigger" → standalone UMD build; "gsap/ScrollTrigger" → ESM), gsap-core ticker (`add` wakes it; listeners get (time s, delta ms, frame); `lagSmoothing(0)` = unlimited threshold; ticker auto-sleeps only while it has < 2 listeners; max 240 fps gap), ScrollTrigger.update(force) (only `true` forces — a Lenis instance argument is a normal update), lenis 1.3.21 README "GSAP ScrollTrigger" + scrollTo docs, lenis.mjs (raf(time ms) advances by delta; scrollTo(HTMLElement) = rect.top + animatedScroll − scroll-margin/padding, uses instance lerp/duration/easing → with `duration: 1.5` set, easing defaults to expo-out and the duration path wins; returns early if stopped/locked), lenis-react.mjs (`useLenis()` falls back to the root store when outside a provider — Navigation/Footer sit outside <LenisProvider>)
  [x] AGENTS.md gate: re-read 01-getting-started/05-server-and-client-components.md "Context providers" (client provider wrapping children from a server layout — LenisProvider's shape; no Next API is used by the 2.0 files otherwise)
  Inventory — ProcessSection (the 2.0 gate: must behave identically):
    P1 ScrollTrigger.create on the wrapper: start "top top", end `+=600%` ((5 nodes + 1) × 100%), pin the section, scrub: true; onUpdate → stage = floor(progress × 6); stage 0 → scrollIndex null, else min(stage − 1, 4)
    P2 on displayIndex change: `.absoluteDescription` clipPath inset(0 0 100% 0) → inset(0), 0.8 s power4.out, overwrite
    P3 `.word` spans: from y 30 / opacity 0 / rotateX −60 → 0, 0.6 s, stagger 0.015, power3.out, delay 0.1, overwrite
    P4 visible images: from x 100vw / scale 0.8·s / opacity 0 / rotation 10 → x 0 / scale s / opacity 1 / rotation 0, 1.2 s, delay cfg.delay || i·0.05, power4.out, overwrite (s = mobileOverride.scale on mobile)
    P5 hover (width ≥ 768): title mouseenter → hoverIndex overrides the scroll index (titleHollow when it differs from the scroll index); leaving the list returns to the scroll index
    P6 inline bg/text colour from the active node (CSS transition 0.6 s); default background image hidden while a node is active
    P7 resize → windowWidth state + ScrollTrigger.refresh() debounced 150 ms; < 768 shows priority-1 images only, < 1024 priority ≤ 2
    P8 registration: module-level `gsap.registerPlugin(ScrollTrigger)` behind a window guard, ScrollTrigger imported from `gsap/dist/ScrollTrigger` — the UMD copy. A second import of `gsap/ScrollTrigger` anywhere else would create a SECOND ScrollTrigger singleton (own trigger list, own scroll listeners), so the recipe's `lenis.on('scroll', ScrollTrigger.update)` would never reach ProcessSection's trigger → all GSAP imports must go through one module.
  Inventory — frame loops + scroll routing today: L1 LenisProvider rAF loop → lenis.raf(time) every frame (1.3b prep); ScrollTrigger hears Lenis only through native scroll events; GSAP's ticker runs its own rAF while tweens are active; N2 nav section links `scrollIntoView({ behavior: 'smooth', block: 'start' })` (menu closes first, hash untouched, `href="#"` stays native); F2 back-to-top `window.scrollTo({ top: 0, behavior: 'smooth' })` — both cut short on this page (D-14)
  Design:
    - src/lib/motion/gsap.ts (no directive; imported only by client modules): `import gsap from 'gsap'`, `import { ScrollTrigger } from 'gsap/ScrollTrigger'`, `import { useGSAP } from '@gsap/react'`; `gsap.registerPlugin(ScrollTrigger, useGSAP)` once; re-exports all three. Ease/duration constants and shared reveal helpers are NOT created yet — they land with their first consumer (2.1+) instead of shipping unused.
    - ProcessSection.tsx: import gsap + ScrollTrigger from `@/lib/motion/gsap`, drop its own registerPlugin block. Choreography, markup, effects untouched (D-2; the rest is 2.5).
    - LenisProvider.tsx: provider rAF loop → recipe: `useLenis()` (root store) gives the instance; effect: `lenis.on('scroll', ScrollTrigger.update)`, `gsap.ticker.add(t => lenis.raf(t * 1000))`, `gsap.ticker.lagSmoothing(0)`; cleanup removes both. Options unchanged (lerp 0.1, duration 1.5, smoothWheel, autoRaf false).
    - D-14: NavigationController + BackToTopButton get the root instance via `useLenis()` and call `lenis.scrollTo(element)` / `lenis.scrollTo(0)`; native smooth scroll stays as the fallback for a click before Lenis exists (first frames after hydration).
  [x] Before capture on the current production build (.next built 2026-09-13T19:42Z at the Phase 1 gate; `git diff 09ec72d HEAD -- src` empty → it IS HEAD's source), port 4310, PID stopped after: scratch process-probe → desktop geometry wrapTop 11738 / pin-spacer 6300 / page 21219; stage 0 pinned (fixed, top 0) no active title, bg rgb(15,15,15); stages 1–5 = 01Discover / 02Define / 03Design / 04Build / 05Launch, muted 4, bg #ff3366 / #00ffcc / #e6ff00 / #ff3300 / #cc00ff, images 21 / 9 / 14 / 13 / 17 all opacity 1 at rest, description clip inset(0%) 48/42/40/32/42 words; back to stage 2 identical; hover 04Build → hollow 04Build, bg #ff3300, 13 images; release → 02Define; above the pin → relative, default bg, bg image visible; past end → relative top −450 with 05Launch. Mobile (page is 466 px wide → innerHeight 1009, pin 844-based): stages 0 / 01Discover 6 imgs / 02Define 6 / 04Build 7 / 05Launch 5 / 05Launch released. 0 console/page errors. Idle rAF census: desktop top 5 loops at 60/s (+1 at 4/s), desktop in process 4 at 60 (+20, +4), mobile top 4 at 60 (+4). Scroll-arrival ×2: desktop Process scrollY 6380 / 6745 (section still 5358 / 4993 px away), back-to-top 0 / 3592, mobile About 14576 (cv top at 165) both runs. Evidence: scratchpad process-before/ (probe.json + 13 stage/hover shots).
  [x] Implemented: src/lib/motion/gsap.ts (single registration + re-exports); ProcessSection imports from it (window-guarded registerPlugin block removed, nothing else); LenisProvider → `useLenis()` + gsap.ticker drives lenis.raf, lenis 'scroll' → ScrollTrigger.update(), lagSmoothing(0), ref + own rAF loop removed; NavigationController + BackToTopButton → `useLenis()` + `lenis.scrollTo(target | 0)` with native fallback before Lenis exists
  [x] Gates: `pnpm build` ✓ (Next 16.2.1 Turbopack, TypeScript clean, / + /_not-found static); `pnpm lint` ✓; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 17 warning(s)` — FAIL img ProcessSection.tsx:181 (same known raw <img> as :186, shifted by the removed lines → 2.5); the `@gsap/react` unimported warning is gone (18 → 17); census 13 client modules (src/lib/motion/gsap.ts carries no directive); placeholders pass. Budget: / JS 11 files 503.1 KB gz / 424.7 KB br / 1758.5 KB raw (+0.8 KB gz vs 502.3: ESM ScrollTrigger + useGSAP now shared; chunking re-split, largest 307.0 KB) | CSS 18.1 KB gz | HTML 12.1 KB gz.
  [x] After-probe on the new build (port 4310, PID stopped): process-probe state diff vs before = 0 across desktop geometry, before-pin, stages 0–5, back to stage 2, above-again, past-end and all mobile stages; the only 2 diffs are the hover samples taken 1.6 s after hover, where the last delayed images are still in the final ~1 % of their 1.2 s power4.out tails (opacity 0.9906 vs 0.9945, same images/order/end scales) — sampling jitter, not behaviour. 0 errors. Idle rAF census: desktop top unchanged (5 loops at 60/s + 1 at 4/s); desktop inside the pin 4 loops at 60 + 4/s (before: + an extra 20/s GSAP loop) → GSAP and Lenis now share one loop; mobile top unchanged. scroll-arrival ×2 (both runs identical): desktop Process → scrollY 11738 = pin start, section top 0, settled 1.49–1.53 s; back-to-top → 0 (1.50 s); mobile About → 14741, cv top 0. links-probe: all 5 desktop + 5 mobile section links land at top 0 in 1.47–1.54 s, hash empty, menu closed, body overflow cleared; mobile social href="#" still native (jump to 0, menu stays open, overflow hidden). Screenshots `.visual/20260914-040702-2.0-motion-after/` 24 desktop + 22 mobile, heights 21219 / 17764, 0 issues; pair sheets `.visual/20260914-040702-2.0-motion-pairs/` (desktop 1–2, mobile 1–2, vs phase1-gate) + scratch process stage sheets — all read: no visible difference (hero particles time-varying).
  [x] Commit `perf(motion): 2.0 single-engine foundation (gsap ticker drives lenis)` (D-9) → LOG → STATE
  [x] ⛔ Owner parity review (V4) — approved 2026-09-14 ("approved, continye")
Phase 1 gate — closed 2026-09-13: owner approved ("Phase 1 Gate is approved. Proceed to Phase 2."). Light-weight question: no change requested with the approval → Capabilities intro + Contact "Let's Build" keep true Inter Light (CSS as authored); restoring weight 400 would be a one-line follow-up if the owner asks later.
Plan for the Phase 1 gate (closed):
  [x] Record owner answers: 1.3b parity approved; D-13 approved; D-14 (section links + back-to-top via lenis.scrollTo in 2.0) approved and appended; INTAKE-1 production domain = sinasotoudeh.ir; INTAKE-5 LinkedIn = linkedin.com/in/sinasotoudeh
  [x] Fresh `pnpm build` (`.next` removed first) ✓ — Next 16.2.1 Turbopack, / and /_not-found static; `pnpm lint` ✓ clean; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 18 warning(s)` — FAIL img ProcessSection.tsx:186 (known, owned by 2.5); warnings: 15 asset sizes, casing dead `resumeData.ts:80 profile.png`, deps `@gsap/react` unimported (used from 2.0), budget target. Same shape as Phase 0 close; client census now enforcing (13 modules), placeholders pass.
  [x] V5 budget: / first-load JS 502.3 KB gz vs BASELINE 504.4 KB gz (−2.1 KB, −0.4%) — no regression; target ≤140 KB binds at 2.8.
  [x] Screenshots `.visual/20260913-194302-phase1-gate/` (Chromium 153.0.8010.12): desktop 1440×900 24 shots, page 21219 px; mobile 390×844@3 22 shots, page 17764 px; 0 console/HTTP issues — page heights identical to phase0-baseline.
  [x] Pair sheets `.visual/20260913-194302-phase1-gate-pairs/`: desktop-1..3-of-3.png (all 24 frames), mobile-1..4-of-4.png (all 22 frames), typography-desktop.png, typography-mobile.png — all read. Every visible difference traced: (1) body text is Inter (narrower → re-wraps in hero burst cards, Capabilities intro, Process panel copy, Resume summary, contact chips) and mono text is JetBrains Mono — 1.1, approved; (2) hero tagline "mark" synthesized italic — 1.1 amendment, owner's call; (3) Works images through the optimizer — no difference visible at sheet scale — 1.2, approved; (4) **newly surfaced, consequence of 1.1:** text styled light (`font-light` on CapabilitiesSection.tsx:115 intro paragraph; `font-weight: 300` on Contact `.title` "Let's Build", Contact.module.css:103/327) now renders as real Inter Light — visibly thinner/dimmer than before, because the old fallback fonts (Arial / DejaVu) have no light face and showed regular weight; in the Capabilities block the shorter paragraph also recentres the "Core Expertise." heading a few px lower. This is the CSS as authored — flagged for the owner's eye, no change made. No regressions found; nothing fixed at the gate. Chrome (nav, menu, footer, cursor) identical, as proven per sub-task. Pre-existing and still present (not Phase 1): mobile hero wordmark/BUILD card overlap at y=844 (display font — 2.1), mobile 466 px overflow clipping the hamburger (2.6).
  [x] LOG line + commit; gate presented — ⛔ hard stop for approval before 2.0
Plan for 1.3b (closed — owner-approved with D-13):
  [x] AGENTS.md gate: re-read 01-getting-started/14-metadata-and-og-images.md (static metadata object, default charset/viewport tags); read 03-api-reference/04-functions/generate-metadata.md (title string/default/template/absolute, description, metadataBase + URL composition); lenis 1.3.21 types + source (node_modules/lenis/dist/lenis-react.d.ts, lenis-react.mjs, lenis.mjs: ReactLenis `options.autoRaf` (prop form deprecated) defaults true; ref = { wrapper, content, lenis }; `lenis.raf(timeMs)` advances by delta and only self-schedules when autoRaf)
  [x] Sources: layout.tsx (metadata still "Create Next App" / "Generated by create next app"); seo-blueprint "Canonical URL wiring" + "Metadata contract" + intake register; copy sources — Hero.tsx:300 "We craft digital experiences that leave a mark.", resumeData.ts:63 role "Front-End Developer & Technical SEO Engineer", resumeData.ts:66 summary, capabilities.ts titles; CustomCursor.tsx + Cursor.module.css (compiled hide rule confirmed `@media (hover:none) and (pointer:coarse){.cursorContainer{display:none}}`); globals.css `body { cursor: none }`, `button { cursor: none }`; LenisProvider.tsx
  Inventory — CustomCursor:
    C1 markup (container + ring + dot) is server-rendered on every device; before the first mousemove both sit at the viewport's top-left corner (visible quarter circles at 0,0 on desktop); hidden by CSS on (hover:none) and (pointer:coarse).
    C2 engine: mousemove stores clientX/Y; a requestAnimationFrame loop runs forever from mount (also on touch devices, also when the mouse is idle): dot = pointer, ring += (pointer − ring) × 0.15 per frame.
    C3 mouseover delegation → state class: text (`[data-cursor="text"]`, input, textarea) → isText; magnetic (`[data-magnetic]`, `.btn-magnetic`) → isMagnetic; hover (a, button, `[data-cursor="hover"]`) → isHovering; else default. CSS transitions size/colour 0.2–0.3s.
    C4 mousedown/up → isClicked (its CSS scale is overridden by the per-frame inline transform — effectively a no-op today; kept as is).
    C5 native cursor is hidden page-wide by globals.css on all devices.
  Inventory — Lenis: L1 `<ReactLenis root options={{ lerp: 0.1, duration: 1.5, smoothWheel: true }}>` around page children; own rAF loop (autoRaf default); no other consumer (`useLenis` unused; ResumeDashboard uses `data-lenis-prevent`); nav section links + back-to-top use native smooth scroll alongside it (1.3a probe numbers are the regression reference).
  Design:
    - src/lib/site.ts: `siteUrl` = NEXT_PUBLIC_SITE_URL (trailing slash trimmed) → `https://${VERCEL_PROJECT_PRODUCTION_URL}` → `http://localhost:3000`. `absoluteUrl()` waits for its first consumer (Phase 3 JSON-LD/sitemap/robots).
    - layout.tsx metadata: `metadataBase: new URL(siteUrl)`, `title: { default: 'Sina Sotoudeh — Front-End Developer & Technical SEO Engineer', template: '%s — Sina Sotoudeh' }`, description drafted from the resume summary (≤160 chars). DRAFT copy (INTAKE-7) — owner sign-off at the Phase 3 gate. OG/Twitter/canonical/icons stay for 3.1 (need INTAKE-4 artwork).
    - CustomCursor (D-13): markup unchanged and still server-rendered; the engine (listeners + loop) runs only where the cursor is visible — the exact complement of the CSS hide rule, re-evaluated on media change — instead of the plan's literal `(pointer: fine)` skip-mount (that would unmount on hover-capable coarse-pointer devices, where CSS still shows the cursor while globals hide the native one → no pointer at all, and would drop the SSR markup). The loop sleeps once the ring has converged (snapped exactly onto the pointer below 0.01 px) and wakes on mousemove — identical pixels, no idle frames. prefers-reduced-motion: the ring sits on the pointer instead of trailing it (intended visible change, reduced-motion users only).
    - LenisProvider: `options.autoRaf: false` + `ref` (LenisRef) + one rAF loop in the provider calling `lenis.raf(time)` — same per-frame advance as Lenis' own loop; 2.0 swaps that loop for gsap.ticker.
  [x] Before capture (production build of HEAD 96baca1): scratch `cursor-probe.mjs` — title "Create Next App"; cursor states isHovering / isMagnetic / isText / isClicked on the expected targets; ring 0.001–0.003 px short of the pointer after 1.2 s; ring x = 582.395 ~4 frames after a 200→1000 jump (same with reduced motion); touch: container display:none but engine running (inline transforms updated by a tap)
  [x] Implemented: src/lib/site.ts (`siteUrl`); layout.tsx metadata (metadataBase, title default/template, description — 60 / 156 chars); CustomCursor engine guard + settle-sleep + reduced-motion follow (D-13, markup unchanged); LenisProvider `options.autoRaf: false` + LenisRef + provider rAF loop calling `lenis.raf(time)`; D-13 appended to DECISIONS.md
  [x] Gates: `pnpm build` ✓ (prerendered <title> and <meta name="description"> present); `pnpm lint` ✓; `verify-portfolio.mjs client crlf casing budget` → `VERIFY: 4 passed, 0 failed, 2 warning(s)`; cursor probe after: states identical, normal-motion lag identical (582.395), ring now settles exactly on the pointer, reduced motion → ring at pointer (1000), touch → no inline transforms (engine off), 0 errors; chrome-probe DOM (nav/menu/footer) still identical; screenshot set `.visual/20260913-192944-1.3b-after/` 24 + 22, heights 21219 / 17764, 0 issues; cursor crops (default/hover/magnetic) and top-left resting cursor identical.
  [x] Scroll-timing investigation (chrome-probe's fixed-wait scroll positions changed after 1.3b): A/B with the old LenisProvider → same shift, so not Lenis; injected no-op / style-writing rAF loops each restore a different number → headless frame-pacing sensitivity; settle-based `scroll-arrival.mjs` on the pre-1.3b build shows the same outcomes as after (desktop "Process" link stops at scrollY 6745 with the section still 4993 px away in both; back-to-top reaches 0 or stops ~3.4–4.0k px in both, run-dependent; mobile About identical). Conclusion: pre-existing interrupted smooth scroll (→ Blockers), not a 1.3b regression.
  [x] Commit `refactor(rsc): 1.3b base metadata, cursor guards, lenis raf prep` — owner-only authorship (D-9)
  [x] Owner parity review (V4) + D-13 approval — 2026-09-13
1.3a — closed 2026-09-13 (commit 50438ed, owner-approved): Navigation/Footer server shells + NavigationController / NewsletterForm / BackToTopButton leaves; client-allowlist.json enforcing.
1.3 split (recorded 2026-09-13, after reading all chrome sources): 1.3a = Navigation + Footer server shells with client leaves + client-allowlist.json (census becomes enforcing); 1.3b = base metadata + src/lib/site.ts, CustomCursor (pointer: fine) + reduced-motion guards, LenisProvider autoRaf:false prep. Reason: 1.3a is the parity-heavy DOM work, 1.3b is config/metadata with its own owner-facing copy; one response each.
Plan for 1.3a:
  [x] AGENTS.md gate: re-read 01-getting-started/05-server-and-client-components.md (Using Client Components, Reducing JS bundle size, Interleaving — server components passed as props/children into a client component)
  [x] Sources: Navigation.tsx (155 lines) + .module.css (331), Footer.tsx (215) + .module.css (396), Cursor.module.css, LenisProvider.tsx, layout.tsx; verify-portfolio.mjs checkClient (allowlist = JSON array of repo-relative paths; fails unlisted 'use client' modules)
  Interaction inventory — Navigation (must survive identically):
    N1 scrolled state: passive scroll listener, rAF-throttled, `.scrolled` on <nav> when scrollY > 50; evaluated once on mount (reload mid-page = scrolled immediately). CSS: bg/border/backdrop blur/shadow/padding transitions 0.4–0.5s.
    N2 section links (desktop <ul> + mobile menu <ul>, hrefs #manifesto #work #process #cv #contact): click → preventDefault (URL hash never changes), close mobile menu, `getElementById(id)?.scrollIntoView({ behavior:'smooth', block:'start' })`; menu closes even if the target is missing.
    N3 hamburger: toggles open; `.hamburgerOpen` X morph (0.4s spring), aria-expanded mirrors state; aria-label "Toggle menu".
    N4 mobile menu overlay: `.mobileMenuOpen` → opacity/visibility 0.6s, li stagger 0.10–0.30s slide-in, `.mobileFooter` fade-up delay 0.5s; aria-hidden = !open; while open body.style.overflow = 'hidden', restored on close/unmount.
    N5 mobile socials `href="#"` (Instagram/Twitter/LinkedIn) have NO handler — native jump to top (+ URL gets "#"). Must stay unhandled.
    N6 logo `next/link` href="/"; CSS-only hovers: navLink underline grow 0.4s, btnGhost/btnPrimary hover, mobile link stroke→fill + translateX. "▶ Reel" and "Start Project" buttons have no handlers (template); `data-cursor`/`data-magnetic` attributes feed CustomCursor states.
  Interaction inventory — Footer:
    F1 newsletter form: controlled email input (required, type=email → native validation bubble), submit → preventDefault, spinner in button + disabled for 1s (simulated — no request is sent), then input cleared.
    F2 back-to-top button: `window.scrollTo({ top: 0, behavior: 'smooth' })`; ≤768px it is position:fixed bottom-right.
    F3 CSS-only: link columns + brand block fadeInUp on first paint (staggered by --col-index), hovers on links/social/legal/backToTop, reduced-motion block. All static text/links/SVG icons, © year from `new Date().getFullYear()`.
  Design: Navigation.tsx → server component rendering all markup; new client leaf `NavigationController.tsx` renders only the stateful elements it must own — <nav> (className N1) with the server-rendered logo/links/CTA passed in as a prop, the hamburger (N3), and the mobile menu wrapper div (N4) with server-rendered menu content as a prop; link clicks (N2) handled by one delegated onClick on each container matching `a[href^="#"]` except `href="#"` (keeps N5 native). Footer.tsx → server component; client leaves `NewsletterForm.tsx` (F1, byte-identical markup) and `BackToTopButton.tsx` (F2). DOM output must be identical to before (verified by outerHTML diff, not just screenshots). Year: now evaluated at build/prerender time instead of also on hydration — same value except across New Year until the next deploy.
  [x] Before capture on the production build of HEAD de73ac4 (scratch `chrome-probe.mjs`, port 4310): outerHTML of nav / mobile menu / footer; N1 (wheel 600 → scrolled), N2 (desktop click Process; mobile tap About → menu closes at once, hash stays empty), N3/N4 (menu open: aria-expanded true, aria-hidden false, body overflow hidden), N5 (Instagram href="#" → native jump to top, menu stays open), F1 (empty submit blocked by native validation; valid submit → disabled + spinner, cleared after ~1s), F2 (back to top → scrollY 0; fixed on mobile); 0 page/console errors
  [x] Implemented: Navigation.tsx is a server component (logo Link, desktop links, CTA, mobile menu content — all markup) passing `bar` + `menu` props into new client leaf NavigationController.tsx (<nav> with scrolled class, hamburger, menu wrapper div, body-overflow effect, one delegated click handler per container for `a[href^="#"]` except `href="#"`). Footer.tsx is a server component; new client leaves NewsletterForm.tsx (form, state, simulated submit kept verbatim, ArrowIcon) and BackToTopButton.tsx (ChevronUpIcon). Logo JSX re-indented only (JSX drops newline whitespace — DOM diff proves it). docs/optimization-state/client-allowlist.json created → census now enforcing.
  [x] Gates: `pnpm build` ✓; `pnpm lint` ✓; `verify-portfolio.mjs client crlf casing budget` → `VERIFY: 4 passed, 0 failed, 2 warning(s)` (census: 13 'use client' modules, all allowlisted; warnings = dead profile.png ref + budget target); after-probe on the new build: dom-nav / dom-menu / dom-footer byte-IDENTICAL to before; every behavior result identical, including in-flight scroll positions (desktop Process scrollY 7316, mobile About 2463 → 14576) and empty error lists; state shots (nav top / scrolled / hover, footer submitting, mobile menu open, mobile footer) visually identical; full set `.visual/20260913-190552-1.3a-chrome-after/` 24 desktop + 22 mobile, heights 21219 / 17764, 0 issues. JS 504.4 → 502.1 KB gz.
  [x] Commit `refactor(rsc): 1.3a server nav/footer shells with client leaves` — owner-only authorship (D-9)
  [ ] Owner parity review (V4) → record approval, then 1.3b
1.2 — closed 2026-09-13 (commit 13a65c1, owner-approved): next.config images → optimizer on, formats AVIF+WebP; Works next/image PNGs now 22–56 KB AVIF. Details: LOG.md + commit body.
1.1 — closed 2026-09-13 (commits 9d3e03a + 2619c24): next/font Inter (normal only, owner dropped italic) + JetBrains Mono feeding --font-body/--font-mono; Resume/Contact family hardcodes → tokens; Contact accent serif stack. Details: LOG.md + commit bodies; parity notes approved (see approvals).
Parity notes pending user review (2.5c, D-25 — pushed with 2.5b):
  - Pin confirmed fixed by the owner. Further mobile work: fewer redraws per stage change (titles/description on their own stable layers, icons drawn once per visit) and off-screen sections release their GPU layers (94 → 68 layers page-wide). No visual change.
  - The emulator can't measure a real phone GPU; next step needs the owner's device model/browser (and ideally a DevTools performance recording) to target what's left.
Parity notes of 2.5b, D-24 (owner 2026-09-23: "the view port is fixed"):
  - The Process section is now held in place by the browser itself (CSS sticky, like the Hero and Capabilities sections) instead of by a script — no more sliding past and snapping back when you scroll into or out of it on a phone. Same scroll length, same stage timing.
  - Phones: the icons' shadows are scaled down with the smaller icons (same look, lighter to draw), and a stage change does less work.
Parity notes of 2.5 ProcessSection, D-23 (owner 2026-09-23: "it got better" — desktop OK; mobile follow-up → 2.5b):
  - Same design and same animations; much lighter: on this machine the desktop scroll through Process went from ~15 fps (median frame 83 ms) to ~50 fps (median 17 ms = full 60 fps frames), the phone profile from 31 to 46 fps with a fifth of the GPU work.
  - Why it was slow: the big background picture carried five stacked shadow filters and every flying icon a blurred shadow on the same element that was scaling/rotating, so the GPU re-computed all those blurs on every frame; icons were re-downloaded/decoded on every pass; the colour change repainted the giant titles. Now shadows are drawn once and the finished pictures are moved.
  - Phones: icons are 60 % of their old size (same places), so they no longer cover the titles and text.
  - Icons now load as optimised modern images and each stage's icons are fetched ahead of time — no pop-in when you arrive at a stage.
  - Icons are still the same artwork (stylised 3D renders); swapping them for real brand icons is a separate option (see chat).
  - What to look at: scroll through the five stages on desktop and phone (colour change, icons flying in, text reveal), hover the titles on desktop.
Parity notes of 2.4b mobile fixes, D-22 (approved 2026-09-23: "it's better now (less laggy)"):
  - Phones: swiping up/down anywhere in Capabilities — over the text or over the cards — scrolls the page (and the cylinder turns with it); swiping sideways on the cards still spins it.
  - Smoother scrolling on phones, page-wide: nothing animates off screen any more (the hero's glowing "SCROLL", the Manifesto strip, the Contact blobs pause while out of view), the Contact background no longer forces repaints while you scroll anywhere on the page, and the cylinder's background moves on the GPU. Measured on a slowed phone profile: main-thread work while scrolling Capabilities roughly halved, slow frames 18 → 4, zero background work when idle.
  - Looks identical everywhere. One side effect on iPhones: Safari used to ignore the Contact section's fixed background (it scrolled with the page there); it now stays fixed like on desktop/Android, as designed.
Parity notes of 2.4 Capabilities cylinder (desktop approved 2026-09-23; mobile → 2.4b):
  - The cylinder is now plain HTML + CSS 3D instead of three.js/WebGL, built from drei's own projection maths: every card lands on the same pixels (measured 0.00 px apart at 9 scroll positions and after a drag, desktop and phone), same fading/shrinking toward the back, same scroll spring, same drag feel and direction, same clipping at the panel edge, same background drift and text slide-in.
  - Noticeable only side by side: the ring follows your scroll ~0.07 s sooner (the WebGL version rendered a frame or two behind).
  - Faster: the page no longer downloads three.js — first-load JavaScript 503 → 264 KB gzipped.
  - Better for search/accessibility: the six capability cards (titles, descriptions, tags) are real page content now, present before any JavaScript runs (before: the section was empty until three.js loaded).
  - With "reduce motion" on: the ring turns directly with scroll (no spring glide), the text is simply there, the background doesn't drift.
  - What to look at: scroll slowly through Capabilities (cards rotating, fading toward the back), drag the cylinder left/right (desktop and phone), hover the front card (colour glow), and compare the text block's slide-in.
Parity notes of 2.3b Selected Works motion (approved 2026-09-23):
  - The section's animations now run on GSAP instead of framer-motion, reproducing framer's exact curves (its default ease-out, the 0.5-bounce spring on the annotation dots, the sheet's custom curve). Measured side by side, every animation has the same shape, duration and end state.
  - The one measurable difference: animations start a frame or few sooner (≈ 15–60 ms; the background swap on hover ≈ 0.1 s sooner after the old one fades), because GSAP starts at once where framer waited for React. Should read as identical, maybe a touch snappier.
  - With "reduce motion" on: project, background and sheet changes are instant (new behaviour).
  - What to look at (desktop): scroll through the three projects (image + description swap, annotation dots popping in with their bounce, lines drawing, labels fading up), hover the dim titles (background cross-fade), scroll the section in and out of view (the right frame's coloured border draws and undraws). Phone: open and close a project sheet (slide up/down).
Parity notes of 2.3a Selected Works (approved 2026-09-23 on production 41b0a4b):
  - Nothing should look or move differently on desktop or phone: same three projects, same highlight/hover/background/image swaps, same phone list and project sheet — measured identical and screenshot-identical.
  - Fixed (D-20): clicking a project title on desktop now always scrolls to that project (e.g. AutoDM from FoladMarket used to stop on FoladMarket while highlighting AutoDM). The scroll is the same 1.5 s glide as the nav links.
  - Faster page load: the two project images no longer download at page load; they load as you approach the section (it's ~4,700 px down). On a slow connection the first project image could appear a moment later when you get there.
  - Phones: the phone layout is in the page from the first paint (before, the desktop layout was sent first and swapped after the page's JavaScript loaded).
  - Not visible: project names are h3 under "Selected Works" (were extra page-level h1s), the sheet's close button is labelled for screen readers.
  - Keyboard access (D-21, your request): Tab reaches every project title (desktop and phone); Enter scrolls to the project / opens the sheet; Escape closes the sheet. Keyboard users now see an accent focus ring site-wide (the template's CSS always intended it, but nothing switched it on); mouse and touch users never see it. The custom cursor now shows its hover ring over the project titles.
  - What to look at: desktop — scroll through the three projects, hover the inactive titles, click each title from each project; phone — tap a project, scroll the sheet, close it.
Parity notes of 2.2 Manifesto (approved 2026-09-23):
  - Nothing should look or move differently: each word still clears from dim + blurred + lowered as you scroll, at the same scroll positions, and "inevitable." still grows to 25× on the way out. Measured identical to the old framer-motion version at 18 scroll points on desktop and phone.
  - Not visible: the text is now plain server HTML read as "Design is not decoration. …" (was "Designisnotdecoration." to screen readers and search engines); "Our Manifesto" is the section's h2 with the exact same look.
  - With "reduce motion" on: the manifesto text is shown still and fully visible (no blur, rise or giant zoom) and the marquee strip stands still. New behaviour.
  - What to look at: scroll slowly from the hero's end through the Manifesto (word-by-word reveal, "inevitable." zoom) and back up; hover the marquee (pauses).
Parity notes of 2.1b (approved 2026-09-22):
  - Desktop (normal screens): nothing should look or move differently. The particle loop now pauses when the hero is more than 200 px off screen and resumes before it comes back.
  - Phones / retina / scaled screens: particles are drawn at up to 2× resolution, so they look crisper (were upscaled from CSS pixels). Measured cost on this box's software GPU: 60 → 44–48 fps with a 4× slowed CPU; real phone GPUs should do better, but judge smoothness on your phone. D-18: keep 2, or 1.5, or 1 (exactly as before).
  - With "reduce motion" on in the OS: the hero shows a still particle picture per state (sphere → cube → scattered field) instead of the animated morph, the cards don't float, the scroll hint doesn't pulse and hides after the first state, and the layers cross-fade without sliding or rising.
  - D-15: all 250 particles are kept on phones (cost measured and now limited to when the hero is on screen).
  - What to look at: phone at the top of the page (particle sharpness, smoothness while scrolling through the hero, moving between hero and Manifesto); desktop hero states 0→3→0 and mouse over the sphere; optionally switch on reduce motion and scroll the hero.
Parity notes of 2.1c (approved 2026-09-22):
  - Phones: the page is exactly screen-wide again (was 466 px wide in a 390 px screen): the nav bar fits with the hamburger fully visible, the menu close X is reachable, and every full-screen section is sized to the real screen height. Hero pin on a 390×844 phone: 3376 px (was 4036), so the hero states come sooner in absolute scroll distance, at the same fractions.
  - Phones: the floating back-to-top button (authored position: fixed, bottom-right, always on under 768 px) now sits at the true screen corner at normal size, also at the top of the page. Unchanged code — say if you want it hidden until you scroll past the hero.
  - Hero: at the first scroll step the big "Sin S" wordmark now rises slightly and fades out, and it comes back when you scroll back to the top. It no longer expands into "Sina Sotoudeh" at the top-left under the nav logo, and it no longer overlaps the BUILD card on phones.
  - What to look at: on a phone, top of page (nav + hamburger), open/close the menu, scroll through the hero and into About/CV (the intro columns still slide in); on desktop, scroll the hero states 0→3→0.
Parity notes of 2.1a (approved 2026-09-22):
  - Nothing should look or move differently. The four hero states switch at the same scroll positions (desktop + phone), the burst-card and project-list staggers run on the same timings, reload in the middle of the hero lands in the same state, the particle engine is untouched.
  - Not visible, for assistive tech and search engines: the big wordmark is now the page's <h1> and reads "Sina Sotoudeh" (it read "SinaSotoudeh" as loose text before); the hero section is labelled by it; the three card titles are h2 (were h4) and the five project names are list items (were h3) — both restyled to the exact same computed size/weight/spacing; the canvas is hidden from screen readers.
  - Under the hood: the hero markup is plain server HTML now; only the state switch (ScrollTrigger, one frame, same as before) and the particle canvas run as client code.
  - What to look at: scroll slowly from the top through the four hero states on desktop and on a phone (sphere + tagline → cards → project list → "Let's build something inevitable."), the card and list staggers, the CTA hover, moving the mouse over the sphere, and reloading the page while in the middle of the hero.
  - Flagged, not changed: the project list ("Nexus Identity, Orbita Core, Zenith Flow, Vega Campaign, Synapse Model" / "Branding … AI Integration") reads like template placeholder copy — your call with the Phase 3 copy sign-off.
Parity notes of 2.0 (approved 2026-09-14):
  - ProcessSection: no visible change intended or found — stage states, colours, images, descriptions, hover override and pin release measured identical; it now imports GSAP from the shared motion module and its ScrollTrigger hears Lenis in the same frame.
  - Wheel/trackpad scrolling: same Lenis settings (lerp 0.1, duration 1.5); it is now stepped by GSAP's frame loop instead of its own — should feel identical.
  - D-14 (intended, visible): nav section links (desktop bar + mobile menu) and the footer back-to-top now always land exactly on their target. Motion is Lenis' programmatic scroll: 1.5 s for any distance with Lenis' expo-out easing (from the site's existing `duration: 1.5` option) — replaces Chrome's native smooth scroll, which varied with distance and stopped ~5,000 px short (desktop Process) or part-way (back-to-top). Long jumps (e.g. Contact from the top, ~19,400 px) pass through the Hero/Process pins within that 1.5 s. Mobile About now lands on the CV section's top (was 165 px short). A wheel/touch during the flight takes over, as before.
  - Invisible edge (disclosed): GSAP lag smoothing is off (Lenis' documented pairing). After a >0.5 s freeze (e.g. returning to a background tab mid-animation) a Process tween jumps to where real time says it should be instead of resuming from its paused frame. GSAP's ticker also caps at 240 fps — only >240 Hz displays would step Lenis less often than before.
  - What to look at: wheel-scroll top → bottom (feel unchanged); in Process scroll through all five stages and hover the titles; from the top click Manifesto / Works / Process / About / Contact; from the footer press back-to-top; on a phone open the menu and tap each link (menu closes, page lands on the section). Run `pnpm build && pnpm start` (or `pnpm dev`).
Parity notes of 1.3b (approved 2026-09-13, kept for the gate summary):
  - Browser tab / bookmarks / search-result title changes from "Create Next App" to the DRAFT "Sina Sotoudeh — Front-End Developer & Technical SEO Engineer" (role line from your resume data, verbatim); meta description from "Generated by create next app" to the DRAFT "Product-minded front-end developer turning UI/UX designs into high-performance, interactive web applications — React, Next.js, TypeScript and technical SEO." (condensed from your resume summary). Both are provisional: final wording is signed off at the Phase 3 gate (INTAKE-7) — edit freely before then.
  - Custom cursor with "reduce motion" turned on in the OS: the ring now sits on the pointer instead of trailing it. Everyone else sees the exact same cursor (states, sizes, trailing speed measured identical); the cursor's loop now pauses when the mouse is still, and phones/tablets without a mouse no longer run the hidden cursor's engine at all (D-13 — needs your approval).
  - Lenis smooth scrolling: now stepped by the app's own frame loop instead of Lenis' internal one (prep for 2.0) — same per-frame behaviour; wheel scrolling should feel identical.
  - What to look at: the tab title; the cursor on desktop over nav links, the Start Project button and the footer email field; wheel-scroll feel; optionally toggle your OS "reduce motion" setting and watch the ring stop trailing.
  - Resume data holds values that look like intake answers — confirmed by owner 2026-09-13 (see Open intake fields).
Parity approvals (recorded):
  - 1.3b draft title/description, reduced-motion cursor, cursor engine guard (D-13), Lenis loop prep — approved by owner 2026-09-13 ("Sub-task 1.3b and decision D-13 are approved.").
  - 0.3 ProcessSection default background now loads (`/images/Process/default.png` casing fix) — approved by owner 2026-09-13 ("background verified").
  - 1.3a server nav/footer shells (no visible change; DOM + behavior identical) — approved by owner 2026-09-13 ("continue").
  - 1.2 optimized Works images (AVIF/WebP via next/image optimizer) — approved by owner 2026-09-13 ("continue").
  - 1.1 typography (all 1.1 parity notes above) — approved by owner 2026-09-13 with one change: "Drop the 52 KB italic font file and continue" → Inter loads normal only; CLS residual accepted as recommended (re-judge with PSI in Phase 6). Amendment verified: build + lint green, verify 5 passed / 0 failed, 2 font preloads (Inter normal 48 KB + JetBrains Mono 40 KB), probe CLS unchanged (desktop 0.0004 / mobile 0.0008 on this box), hero shots `.visual/20260913-184013-1.1b-no-italic/` 0 issues.
Visual baseline (D-10): `.visual/20260913-180311-phase0-baseline/` — production build of commit 84d1f3e source, Chromium 153.0.8010.12, `--every 1vh`: desktop 1440×900 24 shots (page 21219px), mobile 390×844@3 22 shots (page 17764px), 0 console/HTTP issues. Gitignored and local to this box; regenerate from a worktree at 84d1f3e if lost.
  Later sets (same shot grid, 0 issues each): `.visual/20260913-182411-1.1-fonts-after/` (1.1, with the since-dropped Inter italic), `.visual/20260913-184013-1.1b-no-italic/` (top only), `.visual/20260913-184421-1.2-images-after/` (current typography + optimized images), `.visual/20260913-190552-1.3a-chrome-after/` (server nav/footer shells), `.visual/20260913-192944-1.3b-after/` (latest — end of Phase 1 work; the Phase 1 gate compares this against phase0-baseline). Mobile Works images live in the tap-to-open sheet and are not in any set.
  Font rendering on this box — corrected 1.1 by CSS.getPlatformFontsForNode (fc-match was misleading): Chromium renders `'Helvetica Neue', Arial, sans-serif` (display) AND the old body stack in DejaVu Sans; mono → DejaVu Sans Mono; bare serif → DejaVu Serif. Chromium's `local()` resolves neither Arial nor "Liberation Sans" here (only e.g. "DejaVu Sans"), so next/font's `Inter Fallback` (`src: local(Arial)`) reports `error` on this box.
Open intake fields: INTAKE-2, 3, 4, 6, 7 unresolved (register in .claude/skills/portfolio-optimization-architect/references/seo-blueprint.md) — none block Phase 2
  RESOLVED 2026-09-13 by owner:
    INTAKE-5 (2026-09-23, D-19): sameAs profiles = https://www.linkedin.com/in/sinasotoudeh, https://github.com/sinasotoudeh, https://www.instagram.com/sina.sotoude/ (no X/Twitter).
    INTAKE-1 production domain = sinasotoudeh.ir → canonical origin https://sinasotoudeh.ir. Wiring is the blueprint's env var, no code edit: owner sets `NEXT_PUBLIC_SITE_URL=https://sinasotoudeh.ir` on Vercel (Production; Preview too, so previews also point canonicals at production). Until set, src/lib/site.ts falls back to `https://${VERCEL_PROJECT_PRODUCTION_URL}`. Apex vs www: owner gave the apex; confirm the Vercel domain redirects www → apex (or the reverse) before Phase 3 canonicals ship.
    INTAKE-5 sameAs = https://www.linkedin.com/in/sinasotoudeh (owner) + https://github.com/sinasotoudeh (verified from git remote) → Person node in 3.2. The footer's LinkedIn icon still points at the bare linkedin.com home page — visible link change stays with the Phase 3 copy decisions (Blockers).
Budget: BASELINE 504.4 KB gz first-load JS on `/` | CURRENT 264.8 KB gz (verify-portfolio.mjs budget after the 2.5 build; three.js stack no longer bundled — packages still listed until 2.8)
  2.1a detail: /  JS 11 files: 502.5 KB gz / 424.3 KB br / 1755.3 KB raw (−0.6 KB gz: hero markup left the client bundle) | CSS 2 files: 18.1 KB gz | HTML 13.0 KB gz (+0.9)
  2.0 detail: /  JS 11 files: 503.1 KB gz / 424.7 KB br / 1758.5 KB raw (+0.8 KB gz vs 1.3b: shared ESM ScrollTrigger + @gsap/react now bundled; chunks re-split — 307.0 / 62.5 / 43.3 KB gz largest) | CSS 2 files: 18.1 KB gz | HTML 12.1 KB gz
  1.3b detail: /  JS 10 files: 502.3 KB gz / 423.0 KB br / 1757.0 KB raw (+0.2 KB gz: cursor guard/settle logic, Lenis loop) | CSS 2 files: 18.1 KB gz | HTML 12.0 KB gz (real title/description)
  1.3a detail: /  JS 10 files: 502.1 KB gz / 422.8 KB br / 1756.4 KB raw (−2.3 KB gz: nav/footer markup, social SVGs and link data left the client bundle) | CSS 2 files: 18.1 KB gz | HTML 11.9 KB gz (+1.2: that markup now also travels in the inline RSC payload — expected server-component trade)
  1.2 detail: /  JS 10 files: 504.4 KB gz / 424.7 KB br / 1763.5 KB raw | CSS 2 files: 18.1 KB gz | HTML 10.7 KB gz (srcSet URLs). Not in the JS budget but relevant to LCP contention: WorkMinimal's two `priority` images are preloaded on `/` although deep below the fold — now ~AVIF 45–56 KB each instead of the ~1 MB PNG (fix the preload itself in 2.3).
  1.1 detail (after italic drop): /  JS 10 files: 504.4 KB gz / 424.5 KB br / 1763.5 KB raw | CSS 2 files: 18.1 KB gz (+1.0, @font-face rules) | HTML 10.6 KB gz (+0.3, font preload links). Font bytes preloaded on `/`: 88 KB (Inter 48 + JetBrains Mono 40; not counted in the JS budget).
  BASELINE detail (2026-09-13, Next 16.2.1 Turbopack):
    /  JS 10 files: 504.4 KB gz / 424.6 KB br / 1763.5 KB raw | CSS 2 files: 17.1 KB gz | HTML 10.3 KB gz
       349.6 KB gz  static/chunks/0lnsunwr~u0_..js   (1223 KB raw — contains the three.js stack)
        62.5 KB gz  static/chunks/0e_u.-q7b.y8o.js
        37.8 KB gz  static/chunks/0braoh90xzq68.js
    Routes: ○ /   ○ /_not-found   (both prerendered static)
    Target: ≤ 140 KB gz after 2.8 (stretch 120) → −364 KB gz to go
  Verify at Phase 0 close (--all): 8 passed, 1 failed (img, ProcessSection.tsx:186 → 2.5), 18 warnings (15 asset-size, casing: dead `profile.png` ref, deps: @gsap/react kept for 2.0, budget target)
Docs read this run (AGENTS.md gate) — Phase 0 session; every later session re-reads what it uses:
  under node_modules/next/dist/docs/01-app/: 02-guides/upgrading/version-16.md, 01-getting-started/05-server-and-client-components.md, 01-getting-started/12-images.md, 03-api-reference/02-components/image.md, 03-api-reference/05-config/01-next-config-js/images.md, 01-getting-started/13-fonts.md, 03-api-reference/02-components/font.md, 01-getting-started/14-metadata-and-og-images.md, 03-api-reference/03-file-conventions/01-metadata/robots.md, 03-api-reference/03-file-conventions/01-metadata/sitemap.md, 01-getting-started/07-mutating-data.md, 03-api-reference/01-directives/use-server.md, 02-guides/lazy-loading.md, 02-guides/package-bundling.md, 03-api-reference/06-cli/next.md
  1.1 session (2026-09-13): re-read 01-getting-started/13-fonts.md, 03-api-reference/02-components/font.md
  1.2 session (2026-09-13): re-read 01-getting-started/12-images.md, 03-api-reference/02-components/image.md, 03-api-reference/05-config/01-next-config-js/images.md
  1.3a session (2026-09-13): re-read 01-getting-started/05-server-and-client-components.md
  1.3b session (2026-09-13): re-read 01-getting-started/14-metadata-and-og-images.md; read 03-api-reference/04-functions/generate-metadata.md (title, description, metadataBase, URL composition) — Not yet read now: 02-guides/json-ld.md (3.2), 02-guides/forms.md (5.2)
  2.0 session (2026-09-13): re-read 01-getting-started/05-server-and-client-components.md "Context providers"; non-Next sources: @gsap/react 2.1.2 src/types, gsap 3.14.2 exports + gsap-core ticker + ScrollTrigger.update, lenis 1.3.21 README + lenis.mjs + lenis-react.mjs
  Not yet read (as of Phase 0; see per-session lines below): 02-guides/json-ld.md (3.2), 02-guides/forms.md (5.2)
Completed sub-tasks: 0.1, 0.2, 0.3, 0.4, ⛔ Phase 0 gate (plan re-confirmed; D-9 authorship, D-10 visual tooling, D-11 casing check, D-12 gate decisions); 1.1 (owner-approved, italic dropped); 1.2 (owner-approved); 1.3a (owner-approved); 1.3b (owner-approved, D-13 approved); ⛔ Phase 1 gate (owner-approved 2026-09-13); 2.0 (owner-approved); 2.1a (owner-approved 2026-09-22); 2.1c (owner-approved 2026-09-22, D-16/D-17); 2.1b (owner-approved 2026-09-22, D-15/D-18); 2.2 (owner-approved 2026-09-23); 2.2b (owner-requested copy, D-19, approved on production); 2.3a + D-20 + D-21 (owner-approved 2026-09-23); 2.3b (owner-approved 2026-09-23); 2.3c (owner-requested fixes, committed 4d4c0ac); 2.4 (desktop owner-approved 2026-09-23); 2.4b (mobile fixes D-22, owner-approved 2026-09-23); 2.5 (D-23, owner: better); 2.5b (D-24 sticky pin, owner: pin fixed); 2.5c (D-25, pushed — owner phone check pending)
Next Immediate Action: owner verdict on the phone for 2.5c + their device model and browser (iPhone Safari vs Android Chrome decides the next lever). If still heavy: options ready — (a) phones reveal the description words without the 3D flip (opacity + rise), (b) pre-rendered (baked) icon shadows for phones, (c) fewer icons per stage on phones; each a small visible change needing the owner's OK. Then 2.6 Resume.
Blockers / Open Questions:
  - RESOLVED 2026-09-22 → D-15 (keep 250; measured in 2.1b) — (2.1a → 2.1b, owner call — D-15) cwv-invariants "Hero engine hygiene" item 4 asks for fewer particles on phones (≤ 768 px). That is visible (a sparser sphere/field), so it conflicts with parity. Recommendation: keep all 250 particles and take the CPU win from the parity-neutral fixes (pause off-screen, DPR cap, reduced-motion static), measuring the per-frame cost on a 4× CPU-throttled phone profile in 2.1b; only if that cost is material, propose a count with before/after screenshots.
  - (2.1a, Phase 3 copy) Hero project list copy looks like template placeholders ("Nexus Identity"… / "Branding"…).
  - DONE in 2.0 (pending owner parity review) → D-14 (owner-approved 2026-09-13): nav section links + back-to-top route through `lenis.scrollTo(target)`; all 10 links + back-to-top now land on target (before: desktop "Process" stopped ~5000 px short, back-to-top run-dependent, mobile About 165 px short).
  - RESOLVED 2026-09-22 in 2.1c (D-16, owner request) — (1.3a) Mobile horizontal overflow: until the CV section has been reached, the mobile page is 466 px wide in a 390 px viewport (`ResumeDashboard … introRight` sits at right=466 — looks like a slide-in that starts off-screen right). Consequence at the top of the page on phones: the fixed nav stretches to 426 px, the hamburger is clipped at the right edge, and when the menu is opened there the close X is essentially off-screen. Confirmed identical before and after 1.3a (pre-existing). Proposal: fix in 2.6 Resume when its motion is ported (clip or re-origin the slide-in) — a visible improvement, so it needs your OK; please also check on a real phone whether the hamburger looks cut off at the top of the page.
  - (1.3a, owner call — not blocking) The footer newsletter form does not send anything: submit shows a 1 s spinner and clears the field (`// Simulate API call`, now in footer/NewsletterForm.tsx, kept verbatim for parity). Zero-placeholder doctrine says this can't ship as-is at the end of the run. Options: (a) remove the newsletter block, (b) wire it in Phase 5 next to the contact action (Resend audience/contacts — would need a DECISIONS entry), (c) keep it as a pure visual (not recommended). Your pick, by Phase 5.
  - PARTLY RESOLVED 2026-09-23 (D-19: footer + mobile-menu socials now real LinkedIn/GitHub/Instagram; Twitter/Dribbble removed) — remaining: (1.3a, owner call — Phase 3 copy sign-off) Template content still in the chrome besides "Nonato": footer Services/Company links point at routes that don't exist (/services/branding, /services/web, /services/app, /services/ai, /about, /careers, /contact; /work arrives in Phase 4), legal links /privacy /terms /cookies 404, footer social links go to the bare twitter.com / linkedin.com / github.com / dribbble.com home pages, mobile-menu socials are href="#", and the nav "▶ Reel" / "Start Project" buttons do nothing. Needs your real links (INTAKE-5 covers social profiles) or removal decisions; the run won't change visible copy without you.
  - 1.1 CLS residual — ACCEPTED by owner 2026-09-13 as recommended; re-judge with PSI numbers in Phase 6 (kept here as the Phase 6 input). Scratch probe, throttled cold load, no input:
      · as shipped on this box (no local Arial → fallback metrics not applied): desktop 0.0004, mobile 0.0008 — one shift when Inter swaps in; sources: nav links row / Start Project button / logo line + the right-aligned hero tagline.
      · simulated Windows/macOS (fallback face served from Arial-metric Liberation Sans): mobile 0.0000; desktop 0.0004 remains (nav links row + tagline — average-width matching can't make every string identical).
      · implication: Android (no Arial; Roboto) behaves like this box ≈ 0.0008 in the field. Lighthouse lab likely loads the three preloaded fonts before first paint (no swap), but that's unverified until Phase 6.
      · Option if Phase 6 isn't 0.000: a new DECISIONS refinement (number assigned then) — an extra Roboto-metric fallback @font-face (local(Roboto), overrides from next's capsize metrics) after next/font's Arial one; and/or JetBrains Mono `preload: false` (not used in the first viewport) to cut 40 KB of early font bytes. (Inter italic already dropped by the owner.) Neither is applied — both would need a DECISIONS entry + approval. Nav is rebuilt in 1.3, which may move the desktop shift sources anyway.
  - RESOLVED 2026-09-23 → D-19 (Nonato → Sina Sotoudeh). Was: Owner call before the Phase 3 copy sign-off (D-12): the template brand "Nonato" is still visible on the page — `src/components/Manifesto/Manifesto.tsx:22` ("At Nonato,"), `:35` (`NONATO`), `src/components/footer/Footer.tsx:158` ("© … Nonato. All rights reserved."). The run does not change visible copy without the owner's instruction.
Audit delta (final, Phase 0 — vs references/audit-baseline.md @ 8828152):
  - WRONG: "no lockfile of any kind exists" — pnpm-lock.yaml (lockfileVersion 9.0) + pnpm-workspace.yaml tracked and in sync → D-7 (audit-baseline.md corrected in place).
  - WRONG: CRLF count — 30 files, not 20, and working-tree-only (git stored LF via core.autocrlf=input; Vercel never received CRLF) → normalized + .gitattributes in 0.3.
  - MISSED: pnpm-workspace.yaml `ignoredBuiltDependencies: [sharp, unrs-resolver]`.
  - MISSED: stray Shop Platform `.env` at repo root → gone by the Phase 0 gate (D-12).
  - MISSED (Next 16): `next build` no longer reports First Load JS; `next/image` `priority` deprecated for `preload`; `images.qualities` defaults to `[75]` → D-8 / relevant to 1.2.
  - MISSED: runtime casing 404 invisible to the import-only `casing` check (ProcessSection.tsx:175) → fixed in 0.3, check extended (D-11).
  - MISSED: dead data `src/data/resumeData.ts:80` → nonexistent `/images/cv/profile.png`, consumer commented out (ResumeDashboard.tsx:256); `casing` warns; left as-is.
  - MISSED: five unused create-next-app SVGs → removed at the gate (D-12).
  - MISSED: visible "Nonato" copy in Manifesto + Footer (see Blockers) — the audit only noted the package name, which is now `personal-portfolio` (D-12).
  - MISSED: body font stack has no metric-compatible fallback — fixed in 1.1 (next/font size-adjusted fallback).
  - CONFIRMED: @react-three/postprocessing, lucide-react, tailwind-merge dead; autoprefixer unreferenced → removed in 0.4. @gsap/react unimported (kept for 2.0).
  - MISSED (Phase 5): `zod` not installed — added with Resend in 5.1 under D-3.
  - OBSERVED: mobile 390×844 at scrollY 844 — hero wordmark "Sina Sotoudeh" overlaps the BUILD burst card. Still present after 1.1, but 1.1 can't decide it: the wordmark uses `--font-display` (not Inter), which renders as DejaVu Sans Bold on this box — much wider than the Arial/Helvetica real phones use. Needs the owner's eyes on a real phone; belongs to 2.1 Hero if real.
  - TOOLING: verify-portfolio.mjs `deps` check was blind (scanned package.json) → fixed (D-8).
Continuity notes:
  - Remote: pushed by the run at the owner's request — fdb7c39 (2.1c), 18c28c2 (2.1b), 106887a (2.2 + 2.2b), and 2026-09-23 the 2.3a + D-21 commits. Always a fast-forward of main; nothing force-pushed. Earlier: `origin/main` == local `main` at 110d7f0 when 1.1 started — the owner pushed the Phase 0 commits after the gate (the run itself never pushes). Everything from 1.1 on is local-only until the owner pushes.
  - Commits 3632d32…dfcfa7a (0.1–0.2) carry Claude trailers from before D-9; left as-is (no history rewrite unless the owner asks).
  - .claude/settings.local.json is per-machine — deliberately uncommitted. `.visual/` is gitignored.
  - .gitignore ignores `.env*` — Phase 5.1's `.env.example` needs a `!.env.example` negation.
  - Machine: Playwright 1.63.0 tools at ~/.local/share/portfolio-visual-tools, Chromium 153 system deps installed (D-10). Other servers run on this box (owner's next-servers on :3000 and :3001): stop only your own PIDs. This run used :4310.
  - `pnpm build` needs network for next/font/google (fonts are downloaded at build time); Vercel builds have it.
Last Commit: 24f7491 perf(mobile): 2.5b pin ProcessSection with CSS sticky (+ the log-hash commit after it)
