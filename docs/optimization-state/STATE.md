# OPTIMIZATION STATE
Updated: 2026-09-14T04:20:00Z
Approval Mode: per-task
Phase: 2 — Section Rebuilds
Sub-task: 2.0 — Motion foundation (single engine: gsap ticker drives Lenis; D-14 scroll routing)
In Flight: src/lib/motion/gsap.ts (new), src/components/providers/LenisProvider.tsx, src/components/ProcessSection/ProcessSection.tsx (imports only), src/components/navigation/NavigationController.tsx + src/components/footer/BackToTopButton.tsx (D-14)
Status: done-awaiting-approval
Waiting on User Approval: yes — V4 parity review of 2.0: ProcessSection unchanged + D-14 scroll feel (see Parity notes pending user review)
Plan for 2.0:
  [x] Sources read this session: phase-plan Phase 2 contract + 2.0; cwv-invariants "The engine — GSAP consolidation contract"; DECISIONS D-2 / D-13 / D-14; LenisProvider.tsx, ProcessSection.tsx (+ .module.css wrapper/container), NavigationController.tsx, BackToTopButton.tsx, layout.tsx; node_modules: @gsap/react 2.1.2 src + types (useGSAP = gsap.context in a layout effect; `useGSAP.register`), gsap 3.14.2 package exports ("gsap" → ESM index.js; "gsap/dist/ScrollTrigger" → standalone UMD build; "gsap/ScrollTrigger" → ESM), gsap-core ticker (`add` wakes it; listeners get (time s, delta ms, frame); `lagSmoothing(0)` = unlimited threshold; ticker auto-sleeps only while it has < 2 listeners; max 240 fps gap), ScrollTrigger.update(force) (only `true` forces — a Lenis instance argument is a normal update), lenis 1.3.21 README "GSAP ScrollTrigger" + scrollTo docs, lenis.mjs (raf(time ms) advances by delta; scrollTo(HTMLElement) = rect.top + animatedScroll − scroll-margin/padding, uses instance lerp/duration/easing → with `duration: 1.5` set, easing defaults to expo-out and the duration path wins; returns early if stopped/locked), lenis-react.mjs (`useLenis()` falls back to the root store when outside a provider — Navigation/Footer sit outside <LenisProvider>)
  [x] AGENTS.md gate: re-read 01-getting-started/05-server-and-client-components.md "Context providers" (client provider wrapping children from a server layout — LenisProvider's shape; no Next API is used by the 2.0 files otherwise)
  Inventory — ProcessSection (the 2.0 gate: must behave identically):
    P1 ScrollTrigger.create on the wrapper: start "top top", end `+=600%` ((5 nodes + 1) × 100%), pin the section, scrub: true; onUpdate → stage = floor(progress × 6); stage 0 → scrollIndex null, else min(stage − 1, 4)
    P2 on displayIndex change: `.absoluteDescription` clipPath inset(0 0 100% 0) → inset(0), 0.8 s power4.out, overwrite
    P3 `.word` spans: from y 30 / opacity 0 / rotateX −60 → 0, 0.6 s, stagger 0.015, power3.out, delay 0.1, overwrite
    P4 visible images: from x 100vw / scale 0.8·s / opacity 0 / rotation 10 → x 0 / scale s / opacity 1 / rotation 0, 1.2 s, delay cfg.delay || i·0.05, power4.out, overwrite (s = mobileOverride.scale on mobile)
    P5 hover (width ≥ 768): title mouseenter → hoverIndex overrides the scroll index (titleHollow when it differs from the scroll index); leaving the list returns to the scroll index
    P6 inline bg/text colour from the active node (CSS transition 0.6 s); default background image hidden while a node is active
    P7 resize → windowWidth state + ScrollTrigger.refresh() debounced 150 ms; < 768 shows priority-1 images only, < 1024 priority ≤ 2
    P8 registration: module-level `gsap.registerPlugin(ScrollTrigger)` behind a window guard, ScrollTrigger imported from `gsap/dist/ScrollTrigger` — the UMD copy. A second import of `gsap/ScrollTrigger` anywhere else would create a SECOND ScrollTrigger singleton (own trigger list, own scroll listeners), so the recipe's `lenis.on('scroll', ScrollTrigger.update)` would never reach ProcessSection's trigger → all GSAP imports must go through one module.
  Inventory — frame loops + scroll routing today: L1 LenisProvider rAF loop → lenis.raf(time) every frame (1.3b prep); ScrollTrigger hears Lenis only through native scroll events; GSAP's ticker runs its own rAF while tweens are active; N2 nav section links `scrollIntoView({ behavior: 'smooth', block: 'start' })` (menu closes first, hash untouched, `href="#"` stays native); F2 back-to-top `window.scrollTo({ top: 0, behavior: 'smooth' })` — both cut short on this page (D-14)
  Design:
    - src/lib/motion/gsap.ts (no directive; imported only by client modules): `import gsap from 'gsap'`, `import { ScrollTrigger } from 'gsap/ScrollTrigger'`, `import { useGSAP } from '@gsap/react'`; `gsap.registerPlugin(ScrollTrigger, useGSAP)` once; re-exports all three. Ease/duration constants and shared reveal helpers are NOT created yet — they land with their first consumer (2.1+) instead of shipping unused.
    - ProcessSection.tsx: import gsap + ScrollTrigger from `@/lib/motion/gsap`, drop its own registerPlugin block. Choreography, markup, effects untouched (D-2; the rest is 2.5).
    - LenisProvider.tsx: provider rAF loop → recipe: `useLenis()` (root store) gives the instance; effect: `lenis.on('scroll', ScrollTrigger.update)`, `gsap.ticker.add(t => lenis.raf(t * 1000))`, `gsap.ticker.lagSmoothing(0)`; cleanup removes both. Options unchanged (lerp 0.1, duration 1.5, smoothWheel, autoRaf false).
    - D-14: NavigationController + BackToTopButton get the root instance via `useLenis()` and call `lenis.scrollTo(element)` / `lenis.scrollTo(0)`; native smooth scroll stays as the fallback for a click before Lenis exists (first frames after hydration).
  [x] Before capture on the current production build (.next built 2026-09-13T19:42Z at the Phase 1 gate; `git diff 09ec72d HEAD -- src` empty → it IS HEAD's source), port 4310, PID stopped after: scratch process-probe → desktop geometry wrapTop 11738 / pin-spacer 6300 / page 21219; stage 0 pinned (fixed, top 0) no active title, bg rgb(15,15,15); stages 1–5 = 01Discover / 02Define / 03Design / 04Build / 05Launch, muted 4, bg #ff3366 / #00ffcc / #e6ff00 / #ff3300 / #cc00ff, images 21 / 9 / 14 / 13 / 17 all opacity 1 at rest, description clip inset(0%) 48/42/40/32/42 words; back to stage 2 identical; hover 04Build → hollow 04Build, bg #ff3300, 13 images; release → 02Define; above the pin → relative, default bg, bg image visible; past end → relative top −450 with 05Launch. Mobile (page is 466 px wide → innerHeight 1009, pin 844-based): stages 0 / 01Discover 6 imgs / 02Define 6 / 04Build 7 / 05Launch 5 / 05Launch released. 0 console/page errors. Idle rAF census: desktop top 5 loops at 60/s (+1 at 4/s), desktop in process 4 at 60 (+20, +4), mobile top 4 at 60 (+4). Scroll-arrival ×2: desktop Process scrollY 6380 / 6745 (section still 5358 / 4993 px away), back-to-top 0 / 3592, mobile About 14576 (cv top at 165) both runs. Evidence: scratchpad process-before/ (probe.json + 13 stage/hover shots).
  [x] Implemented: src/lib/motion/gsap.ts (single registration + re-exports); ProcessSection imports from it (window-guarded registerPlugin block removed, nothing else); LenisProvider → `useLenis()` + gsap.ticker drives lenis.raf, lenis 'scroll' → ScrollTrigger.update(), lagSmoothing(0), ref + own rAF loop removed; NavigationController + BackToTopButton → `useLenis()` + `lenis.scrollTo(target | 0)` with native fallback before Lenis exists
  [x] Gates: `pnpm build` ✓ (Next 16.2.1 Turbopack, TypeScript clean, / + /_not-found static); `pnpm lint` ✓; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 17 warning(s)` — FAIL img ProcessSection.tsx:181 (same known raw <img> as :186, shifted by the removed lines → 2.5); the `@gsap/react` unimported warning is gone (18 → 17); census 13 client modules (src/lib/motion/gsap.ts carries no directive); placeholders pass. Budget: / JS 11 files 503.1 KB gz / 424.7 KB br / 1758.5 KB raw (+0.8 KB gz vs 502.3: ESM ScrollTrigger + useGSAP now shared; chunking re-split, largest 307.0 KB) | CSS 18.1 KB gz | HTML 12.1 KB gz.
  [x] After-probe on the new build (port 4310, PID stopped): process-probe state diff vs before = 0 across desktop geometry, before-pin, stages 0–5, back to stage 2, above-again, past-end and all mobile stages; the only 2 diffs are the hover samples taken 1.6 s after hover, where the last delayed images are still in the final ~1 % of their 1.2 s power4.out tails (opacity 0.9906 vs 0.9945, same images/order/end scales) — sampling jitter, not behaviour. 0 errors. Idle rAF census: desktop top unchanged (5 loops at 60/s + 1 at 4/s); desktop inside the pin 4 loops at 60 + 4/s (before: + an extra 20/s GSAP loop) → GSAP and Lenis now share one loop; mobile top unchanged. scroll-arrival ×2 (both runs identical): desktop Process → scrollY 11738 = pin start, section top 0, settled 1.49–1.53 s; back-to-top → 0 (1.50 s); mobile About → 14741, cv top 0. links-probe: all 5 desktop + 5 mobile section links land at top 0 in 1.47–1.54 s, hash empty, menu closed, body overflow cleared; mobile social href="#" still native (jump to 0, menu stays open, overflow hidden). Screenshots `.visual/20260914-040702-2.0-motion-after/` 24 desktop + 22 mobile, heights 21219 / 17764, 0 issues; pair sheets `.visual/20260914-040702-2.0-motion-pairs/` (desktop 1–2, mobile 1–2, vs phase1-gate) + scratch process stage sheets — all read: no visible difference (hero particles time-varying).
  [x] Commit `perf(motion): 2.0 single-engine foundation (gsap ticker drives lenis)` (D-9) → LOG → STATE
  [ ] ⛔ Owner parity review (V4) → record approval, then 2.1 Hero
Phase 1 gate — closed 2026-09-13: owner approved ("Phase 1 Gate is approved. Proceed to Phase 2."). Light-weight question: no change requested with the approval → Capabilities intro + Contact "Let's Build" keep true Inter Light (CSS as authored); restoring weight 400 would be a one-line follow-up if the owner asks later.
Plan for the Phase 1 gate (closed):
  [x] Record owner answers: 1.3b parity approved; D-13 approved; D-14 (section links + back-to-top via lenis.scrollTo in 2.0) approved and appended; INTAKE-1 production domain = sinasotoudeh.ir; INTAKE-5 LinkedIn = linkedin.com/in/sinasotoudeh
  [x] Fresh `pnpm build` (`.next` removed first) ✓ — Next 16.2.1 Turbopack, / and /_not-found static; `pnpm lint` ✓ clean; `verify-portfolio.mjs --all` → `VERIFY: 8 passed, 1 failed, 18 warning(s)` — FAIL img ProcessSection.tsx:186 (known, owned by 2.5); warnings: 15 asset sizes, casing dead `resumeData.ts:80 profile.png`, deps `@gsap/react` unimported (used from 2.0), budget target. Same shape as Phase 0 close; client census now enforcing (13 modules), placeholders pass.
  [x] V5 budget: / first-load JS 502.3 KB gz vs BASELINE 504.4 KB gz (−2.1 KB, −0.4%) — no regression; target ≤140 KB binds at 2.8.
  [x] Screenshots `.visual/20260913-194302-phase1-gate/` (Chromium 153.0.8010.12): desktop 1440×900 24 shots, page 21219 px; mobile 390×844@3 22 shots, page 17764 px; 0 console/HTTP issues — page heights identical to phase0-baseline.
  [x] Pair sheets `.visual/20260913-194302-phase1-gate-pairs/`: desktop-1..3-of-3.png (all 24 frames), mobile-1..4-of-4.png (all 22 frames), typography-desktop.png, typography-mobile.png — all read. Every visible difference traced: (1) body text is Inter (narrower → re-wraps in hero burst cards, Capabilities intro, Process panel copy, Resume summary, contact chips) and mono text is JetBrains Mono — 1.1, approved; (2) hero tagline "mark" synthesized italic — 1.1 amendment, owner's call; (3) Works images through the optimizer — no difference visible at sheet scale — 1.2, approved; (4) **newly surfaced, consequence of 1.1:** text styled light (`font-light` on CapabilitiesSection.tsx:115 intro paragraph; `font-weight: 300` on Contact `.title` "Let's Build", Contact.module.css:103/327) now renders as real Inter Light — visibly thinner/dimmer than before, because the old fallback fonts (Arial / DejaVu) have no light face and showed regular weight; in the Capabilities block the shorter paragraph also recentres the "Core Expertise." heading a few px lower. This is the CSS as authored — flagged for the owner's eye, no change made. No regressions found; nothing fixed at the gate. Chrome (nav, menu, footer, cursor) identical, as proven per sub-task. Pre-existing and still present (not Phase 1): mobile hero wordmark/BUILD card overlap at y=844 (display font — 2.1), mobile 466 px overflow clipping the hamburger (2.6).
  [x] LOG line + commit; gate presented — ⛔ hard stop for approval before 2.0
Plan for 1.3b (closed — owner-approved with D-13):
  [x] AGENTS.md gate: re-read 01-getting-started/14-metadata-and-og-images.md (static metadata object, default charset/viewport tags); read 03-api-reference/04-functions/generate-metadata.md (title string/default/template/absolute, description, metadataBase + URL composition); lenis 1.3.21 types + source (node_modules/lenis/dist/lenis-react.d.ts, lenis-react.mjs, lenis.mjs: ReactLenis `options.autoRaf` (prop form deprecated) defaults true; ref = { wrapper, content, lenis }; `lenis.raf(timeMs)` advances by delta and only self-schedules when autoRaf)
  [x] Sources: layout.tsx (metadata still "Create Next App" / "Generated by create next app"); seo-blueprint "Canonical URL wiring" + "Metadata contract" + intake register; copy sources — Hero.tsx:300 "We craft digital experiences that leave a mark.", resumeData.ts:63 role "Front-End Developer & Technical SEO Engineer", resumeData.ts:66 summary, capabilities.ts titles; CustomCursor.tsx + Cursor.module.css (compiled hide rule confirmed `@media (hover:none) and (pointer:coarse){.cursorContainer{display:none}}`); globals.css `body { cursor: none }`, `button { cursor: none }`; LenisProvider.tsx
  Inventory — CustomCursor:
    C1 markup (container + ring + dot) is server-rendered on every device; before the first mousemove both sit at the viewport's top-left corner (visible quarter circles at 0,0 on desktop); hidden by CSS on (hover:none) and (pointer:coarse).
    C2 engine: mousemove stores clientX/Y; a requestAnimationFrame loop runs forever from mount (also on touch devices, also when the mouse is idle): dot = pointer, ring += (pointer − ring) × 0.15 per frame.
    C3 mouseover delegation → state class: text (`[data-cursor="text"]`, input, textarea) → isText; magnetic (`[data-magnetic]`, `.btn-magnetic`) → isMagnetic; hover (a, button, `[data-cursor="hover"]`) → isHovering; else default. CSS transitions size/colour 0.2–0.3s.
    C4 mousedown/up → isClicked (its CSS scale is overridden by the per-frame inline transform — effectively a no-op today; kept as is).
    C5 native cursor is hidden page-wide by globals.css on all devices.
  Inventory — Lenis: L1 `<ReactLenis root options={{ lerp: 0.1, duration: 1.5, smoothWheel: true }}>` around page children; own rAF loop (autoRaf default); no other consumer (`useLenis` unused; ResumeDashboard uses `data-lenis-prevent`); nav section links + back-to-top use native smooth scroll alongside it (1.3a probe numbers are the regression reference).
  Design:
    - src/lib/site.ts: `siteUrl` = NEXT_PUBLIC_SITE_URL (trailing slash trimmed) → `https://${VERCEL_PROJECT_PRODUCTION_URL}` → `http://localhost:3000`. `absoluteUrl()` waits for its first consumer (Phase 3 JSON-LD/sitemap/robots).
    - layout.tsx metadata: `metadataBase: new URL(siteUrl)`, `title: { default: 'Sina Sotoudeh — Front-End Developer & Technical SEO Engineer', template: '%s — Sina Sotoudeh' }`, description drafted from the resume summary (≤160 chars). DRAFT copy (INTAKE-7) — owner sign-off at the Phase 3 gate. OG/Twitter/canonical/icons stay for 3.1 (need INTAKE-4 artwork).
    - CustomCursor (D-13): markup unchanged and still server-rendered; the engine (listeners + loop) runs only where the cursor is visible — the exact complement of the CSS hide rule, re-evaluated on media change — instead of the plan's literal `(pointer: fine)` skip-mount (that would unmount on hover-capable coarse-pointer devices, where CSS still shows the cursor while globals hide the native one → no pointer at all, and would drop the SSR markup). The loop sleeps once the ring has converged (snapped exactly onto the pointer below 0.01 px) and wakes on mousemove — identical pixels, no idle frames. prefers-reduced-motion: the ring sits on the pointer instead of trailing it (intended visible change, reduced-motion users only).
    - LenisProvider: `options.autoRaf: false` + `ref` (LenisRef) + one rAF loop in the provider calling `lenis.raf(time)` — same per-frame advance as Lenis' own loop; 2.0 swaps that loop for gsap.ticker.
  [x] Before capture (production build of HEAD 96baca1): scratch `cursor-probe.mjs` — title "Create Next App"; cursor states isHovering / isMagnetic / isText / isClicked on the expected targets; ring 0.001–0.003 px short of the pointer after 1.2 s; ring x = 582.395 ~4 frames after a 200→1000 jump (same with reduced motion); touch: container display:none but engine running (inline transforms updated by a tap)
  [x] Implemented: src/lib/site.ts (`siteUrl`); layout.tsx metadata (metadataBase, title default/template, description — 60 / 156 chars); CustomCursor engine guard + settle-sleep + reduced-motion follow (D-13, markup unchanged); LenisProvider `options.autoRaf: false` + LenisRef + provider rAF loop calling `lenis.raf(time)`; D-13 appended to DECISIONS.md
  [x] Gates: `pnpm build` ✓ (prerendered <title> and <meta name="description"> present); `pnpm lint` ✓; `verify-portfolio.mjs client crlf casing budget` → `VERIFY: 4 passed, 0 failed, 2 warning(s)`; cursor probe after: states identical, normal-motion lag identical (582.395), ring now settles exactly on the pointer, reduced motion → ring at pointer (1000), touch → no inline transforms (engine off), 0 errors; chrome-probe DOM (nav/menu/footer) still identical; screenshot set `.visual/20260913-192944-1.3b-after/` 24 + 22, heights 21219 / 17764, 0 issues; cursor crops (default/hover/magnetic) and top-left resting cursor identical.
  [x] Scroll-timing investigation (chrome-probe's fixed-wait scroll positions changed after 1.3b): A/B with the old LenisProvider → same shift, so not Lenis; injected no-op / style-writing rAF loops each restore a different number → headless frame-pacing sensitivity; settle-based `scroll-arrival.mjs` on the pre-1.3b build shows the same outcomes as after (desktop "Process" link stops at scrollY 6745 with the section still 4993 px away in both; back-to-top reaches 0 or stops ~3.4–4.0k px in both, run-dependent; mobile About identical). Conclusion: pre-existing interrupted smooth scroll (→ Blockers), not a 1.3b regression.
  [x] Commit `refactor(rsc): 1.3b base metadata, cursor guards, lenis raf prep` — owner-only authorship (D-9)
  [x] Owner parity review (V4) + D-13 approval — 2026-09-13
1.3a — closed 2026-09-13 (commit 50438ed, owner-approved): Navigation/Footer server shells + NavigationController / NewsletterForm / BackToTopButton leaves; client-allowlist.json enforcing.
1.3 split (recorded 2026-09-13, after reading all chrome sources): 1.3a = Navigation + Footer server shells with client leaves + client-allowlist.json (census becomes enforcing); 1.3b = base metadata + src/lib/site.ts, CustomCursor (pointer: fine) + reduced-motion guards, LenisProvider autoRaf:false prep. Reason: 1.3a is the parity-heavy DOM work, 1.3b is config/metadata with its own owner-facing copy; one response each.
Plan for 1.3a:
  [x] AGENTS.md gate: re-read 01-getting-started/05-server-and-client-components.md (Using Client Components, Reducing JS bundle size, Interleaving — server components passed as props/children into a client component)
  [x] Sources: Navigation.tsx (155 lines) + .module.css (331), Footer.tsx (215) + .module.css (396), Cursor.module.css, LenisProvider.tsx, layout.tsx; verify-portfolio.mjs checkClient (allowlist = JSON array of repo-relative paths; fails unlisted 'use client' modules)
  Interaction inventory — Navigation (must survive identically):
    N1 scrolled state: passive scroll listener, rAF-throttled, `.scrolled` on <nav> when scrollY > 50; evaluated once on mount (reload mid-page = scrolled immediately). CSS: bg/border/backdrop blur/shadow/padding transitions 0.4–0.5s.
    N2 section links (desktop <ul> + mobile menu <ul>, hrefs #manifesto #work #process #cv #contact): click → preventDefault (URL hash never changes), close mobile menu, `getElementById(id)?.scrollIntoView({ behavior:'smooth', block:'start' })`; menu closes even if the target is missing.
    N3 hamburger: toggles open; `.hamburgerOpen` X morph (0.4s spring), aria-expanded mirrors state; aria-label "Toggle menu".
    N4 mobile menu overlay: `.mobileMenuOpen` → opacity/visibility 0.6s, li stagger 0.10–0.30s slide-in, `.mobileFooter` fade-up delay 0.5s; aria-hidden = !open; while open body.style.overflow = 'hidden', restored on close/unmount.
    N5 mobile socials `href="#"` (Instagram/Twitter/LinkedIn) have NO handler — native jump to top (+ URL gets "#"). Must stay unhandled.
    N6 logo `next/link` href="/"; CSS-only hovers: navLink underline grow 0.4s, btnGhost/btnPrimary hover, mobile link stroke→fill + translateX. "▶ Reel" and "Start Project" buttons have no handlers (template); `data-cursor`/`data-magnetic` attributes feed CustomCursor states.
  Interaction inventory — Footer:
    F1 newsletter form: controlled email input (required, type=email → native validation bubble), submit → preventDefault, spinner in button + disabled for 1s (simulated — no request is sent), then input cleared.
    F2 back-to-top button: `window.scrollTo({ top: 0, behavior: 'smooth' })`; ≤768px it is position:fixed bottom-right.
    F3 CSS-only: link columns + brand block fadeInUp on first paint (staggered by --col-index), hovers on links/social/legal/backToTop, reduced-motion block. All static text/links/SVG icons, © year from `new Date().getFullYear()`.
  Design: Navigation.tsx → server component rendering all markup; new client leaf `NavigationController.tsx` renders only the stateful elements it must own — <nav> (className N1) with the server-rendered logo/links/CTA passed in as a prop, the hamburger (N3), and the mobile menu wrapper div (N4) with server-rendered menu content as a prop; link clicks (N2) handled by one delegated onClick on each container matching `a[href^="#"]` except `href="#"` (keeps N5 native). Footer.tsx → server component; client leaves `NewsletterForm.tsx` (F1, byte-identical markup) and `BackToTopButton.tsx` (F2). DOM output must be identical to before (verified by outerHTML diff, not just screenshots). Year: now evaluated at build/prerender time instead of also on hydration — same value except across New Year until the next deploy.
  [x] Before capture on the production build of HEAD de73ac4 (scratch `chrome-probe.mjs`, port 4310): outerHTML of nav / mobile menu / footer; N1 (wheel 600 → scrolled), N2 (desktop click Process; mobile tap About → menu closes at once, hash stays empty), N3/N4 (menu open: aria-expanded true, aria-hidden false, body overflow hidden), N5 (Instagram href="#" → native jump to top, menu stays open), F1 (empty submit blocked by native validation; valid submit → disabled + spinner, cleared after ~1s), F2 (back to top → scrollY 0; fixed on mobile); 0 page/console errors
  [x] Implemented: Navigation.tsx is a server component (logo Link, desktop links, CTA, mobile menu content — all markup) passing `bar` + `menu` props into new client leaf NavigationController.tsx (<nav> with scrolled class, hamburger, menu wrapper div, body-overflow effect, one delegated click handler per container for `a[href^="#"]` except `href="#"`). Footer.tsx is a server component; new client leaves NewsletterForm.tsx (form, state, simulated submit kept verbatim, ArrowIcon) and BackToTopButton.tsx (ChevronUpIcon). Logo JSX re-indented only (JSX drops newline whitespace — DOM diff proves it). docs/optimization-state/client-allowlist.json created → census now enforcing.
  [x] Gates: `pnpm build` ✓; `pnpm lint` ✓; `verify-portfolio.mjs client crlf casing budget` → `VERIFY: 4 passed, 0 failed, 2 warning(s)` (census: 13 'use client' modules, all allowlisted; warnings = dead profile.png ref + budget target); after-probe on the new build: dom-nav / dom-menu / dom-footer byte-IDENTICAL to before; every behavior result identical, including in-flight scroll positions (desktop Process scrollY 7316, mobile About 2463 → 14576) and empty error lists; state shots (nav top / scrolled / hover, footer submitting, mobile menu open, mobile footer) visually identical; full set `.visual/20260913-190552-1.3a-chrome-after/` 24 desktop + 22 mobile, heights 21219 / 17764, 0 issues. JS 504.4 → 502.1 KB gz.
  [x] Commit `refactor(rsc): 1.3a server nav/footer shells with client leaves` — owner-only authorship (D-9)
  [ ] Owner parity review (V4) → record approval, then 1.3b
1.2 — closed 2026-09-13 (commit 13a65c1, owner-approved): next.config images → optimizer on, formats AVIF+WebP; Works next/image PNGs now 22–56 KB AVIF. Details: LOG.md + commit body.
1.1 — closed 2026-09-13 (commits 9d3e03a + 2619c24): next/font Inter (normal only, owner dropped italic) + JetBrains Mono feeding --font-body/--font-mono; Resume/Contact family hardcodes → tokens; Contact accent serif stack. Details: LOG.md + commit bodies; parity notes approved (see approvals).
Parity notes pending user review (2.0):
  - ProcessSection: no visible change intended or found — stage states, colours, images, descriptions, hover override and pin release measured identical; it now imports GSAP from the shared motion module and its ScrollTrigger hears Lenis in the same frame.
  - Wheel/trackpad scrolling: same Lenis settings (lerp 0.1, duration 1.5); it is now stepped by GSAP's frame loop instead of its own — should feel identical.
  - D-14 (intended, visible): nav section links (desktop bar + mobile menu) and the footer back-to-top now always land exactly on their target. Motion is Lenis' programmatic scroll: 1.5 s for any distance with Lenis' expo-out easing (from the site's existing `duration: 1.5` option) — replaces Chrome's native smooth scroll, which varied with distance and stopped ~5,000 px short (desktop Process) or part-way (back-to-top). Long jumps (e.g. Contact from the top, ~19,400 px) pass through the Hero/Process pins within that 1.5 s. Mobile About now lands on the CV section's top (was 165 px short). A wheel/touch during the flight takes over, as before.
  - Invisible edge (disclosed): GSAP lag smoothing is off (Lenis' documented pairing). After a >0.5 s freeze (e.g. returning to a background tab mid-animation) a Process tween jumps to where real time says it should be instead of resuming from its paused frame. GSAP's ticker also caps at 240 fps — only >240 Hz displays would step Lenis less often than before.
  - What to look at: wheel-scroll top → bottom (feel unchanged); in Process scroll through all five stages and hover the titles; from the top click Manifesto / Works / Process / About / Contact; from the footer press back-to-top; on a phone open the menu and tap each link (menu closes, page lands on the section). Run `pnpm build && pnpm start` (or `pnpm dev`).
Parity notes of 1.3b (approved 2026-09-13, kept for the gate summary):
  - Browser tab / bookmarks / search-result title changes from "Create Next App" to the DRAFT "Sina Sotoudeh — Front-End Developer & Technical SEO Engineer" (role line from your resume data, verbatim); meta description from "Generated by create next app" to the DRAFT "Product-minded front-end developer turning UI/UX designs into high-performance, interactive web applications — React, Next.js, TypeScript and technical SEO." (condensed from your resume summary). Both are provisional: final wording is signed off at the Phase 3 gate (INTAKE-7) — edit freely before then.
  - Custom cursor with "reduce motion" turned on in the OS: the ring now sits on the pointer instead of trailing it. Everyone else sees the exact same cursor (states, sizes, trailing speed measured identical); the cursor's loop now pauses when the mouse is still, and phones/tablets without a mouse no longer run the hidden cursor's engine at all (D-13 — needs your approval).
  - Lenis smooth scrolling: now stepped by the app's own frame loop instead of Lenis' internal one (prep for 2.0) — same per-frame behaviour; wheel scrolling should feel identical.
  - What to look at: the tab title; the cursor on desktop over nav links, the Start Project button and the footer email field; wheel-scroll feel; optionally toggle your OS "reduce motion" setting and watch the ring stop trailing.
  - Resume data holds values that look like intake answers — confirmed by owner 2026-09-13 (see Open intake fields).
Parity approvals (recorded):
  - 1.3b draft title/description, reduced-motion cursor, cursor engine guard (D-13), Lenis loop prep — approved by owner 2026-09-13 ("Sub-task 1.3b and decision D-13 are approved.").
  - 0.3 ProcessSection default background now loads (`/images/Process/default.png` casing fix) — approved by owner 2026-09-13 ("background verified").
  - 1.3a server nav/footer shells (no visible change; DOM + behavior identical) — approved by owner 2026-09-13 ("continue").
  - 1.2 optimized Works images (AVIF/WebP via next/image optimizer) — approved by owner 2026-09-13 ("continue").
  - 1.1 typography (all 1.1 parity notes above) — approved by owner 2026-09-13 with one change: "Drop the 52 KB italic font file and continue" → Inter loads normal only; CLS residual accepted as recommended (re-judge with PSI in Phase 6). Amendment verified: build + lint green, verify 5 passed / 0 failed, 2 font preloads (Inter normal 48 KB + JetBrains Mono 40 KB), probe CLS unchanged (desktop 0.0004 / mobile 0.0008 on this box), hero shots `.visual/20260913-184013-1.1b-no-italic/` 0 issues.
Visual baseline (D-10): `.visual/20260913-180311-phase0-baseline/` — production build of commit 84d1f3e source, Chromium 153.0.8010.12, `--every 1vh`: desktop 1440×900 24 shots (page 21219px), mobile 390×844@3 22 shots (page 17764px), 0 console/HTTP issues. Gitignored and local to this box; regenerate from a worktree at 84d1f3e if lost.
  Later sets (same shot grid, 0 issues each): `.visual/20260913-182411-1.1-fonts-after/` (1.1, with the since-dropped Inter italic), `.visual/20260913-184013-1.1b-no-italic/` (top only), `.visual/20260913-184421-1.2-images-after/` (current typography + optimized images), `.visual/20260913-190552-1.3a-chrome-after/` (server nav/footer shells), `.visual/20260913-192944-1.3b-after/` (latest — end of Phase 1 work; the Phase 1 gate compares this against phase0-baseline). Mobile Works images live in the tap-to-open sheet and are not in any set.
  Font rendering on this box — corrected 1.1 by CSS.getPlatformFontsForNode (fc-match was misleading): Chromium renders `'Helvetica Neue', Arial, sans-serif` (display) AND the old body stack in DejaVu Sans; mono → DejaVu Sans Mono; bare serif → DejaVu Serif. Chromium's `local()` resolves neither Arial nor "Liberation Sans" here (only e.g. "DejaVu Sans"), so next/font's `Inter Fallback` (`src: local(Arial)`) reports `error` on this box.
Open intake fields: INTAKE-2, 3, 4, 6, 7 unresolved (register in .claude/skills/portfolio-optimization-architect/references/seo-blueprint.md) — none block Phase 2
  RESOLVED 2026-09-13 by owner:
    INTAKE-1 production domain = sinasotoudeh.ir → canonical origin https://sinasotoudeh.ir. Wiring is the blueprint's env var, no code edit: owner sets `NEXT_PUBLIC_SITE_URL=https://sinasotoudeh.ir` on Vercel (Production; Preview too, so previews also point canonicals at production). Until set, src/lib/site.ts falls back to `https://${VERCEL_PROJECT_PRODUCTION_URL}`. Apex vs www: owner gave the apex; confirm the Vercel domain redirects www → apex (or the reverse) before Phase 3 canonicals ship.
    INTAKE-5 sameAs = https://www.linkedin.com/in/sinasotoudeh (owner) + https://github.com/sinasotoudeh (verified from git remote) → Person node in 3.2. The footer's LinkedIn icon still points at the bare linkedin.com home page — visible link change stays with the Phase 3 copy decisions (Blockers).
Budget: BASELINE 504.4 KB gz first-load JS on `/` | CURRENT 503.1 KB gz (verify-portfolio.mjs budget after the 2.0 build)
  2.0 detail: /  JS 11 files: 503.1 KB gz / 424.7 KB br / 1758.5 KB raw (+0.8 KB gz vs 1.3b: shared ESM ScrollTrigger + @gsap/react now bundled; chunks re-split — 307.0 / 62.5 / 43.3 KB gz largest) | CSS 2 files: 18.1 KB gz | HTML 12.1 KB gz
  1.3b detail: /  JS 10 files: 502.3 KB gz / 423.0 KB br / 1757.0 KB raw (+0.2 KB gz: cursor guard/settle logic, Lenis loop) | CSS 2 files: 18.1 KB gz | HTML 12.0 KB gz (real title/description)
  1.3a detail: /  JS 10 files: 502.1 KB gz / 422.8 KB br / 1756.4 KB raw (−2.3 KB gz: nav/footer markup, social SVGs and link data left the client bundle) | CSS 2 files: 18.1 KB gz | HTML 11.9 KB gz (+1.2: that markup now also travels in the inline RSC payload — expected server-component trade)
  1.2 detail: /  JS 10 files: 504.4 KB gz / 424.7 KB br / 1763.5 KB raw | CSS 2 files: 18.1 KB gz | HTML 10.7 KB gz (srcSet URLs). Not in the JS budget but relevant to LCP contention: WorkMinimal's two `priority` images are preloaded on `/` although deep below the fold — now ~AVIF 45–56 KB each instead of the ~1 MB PNG (fix the preload itself in 2.3).
  1.1 detail (after italic drop): /  JS 10 files: 504.4 KB gz / 424.5 KB br / 1763.5 KB raw | CSS 2 files: 18.1 KB gz (+1.0, @font-face rules) | HTML 10.6 KB gz (+0.3, font preload links). Font bytes preloaded on `/`: 88 KB (Inter 48 + JetBrains Mono 40; not counted in the JS budget).
  BASELINE detail (2026-09-13, Next 16.2.1 Turbopack):
    /  JS 10 files: 504.4 KB gz / 424.6 KB br / 1763.5 KB raw | CSS 2 files: 17.1 KB gz | HTML 10.3 KB gz
       349.6 KB gz  static/chunks/0lnsunwr~u0_..js   (1223 KB raw — contains the three.js stack)
        62.5 KB gz  static/chunks/0e_u.-q7b.y8o.js
        37.8 KB gz  static/chunks/0braoh90xzq68.js
    Routes: ○ /   ○ /_not-found   (both prerendered static)
    Target: ≤ 140 KB gz after 2.8 (stretch 120) → −364 KB gz to go
  Verify at Phase 0 close (--all): 8 passed, 1 failed (img, ProcessSection.tsx:186 → 2.5), 18 warnings (15 asset-size, casing: dead `profile.png` ref, deps: @gsap/react kept for 2.0, budget target)
Docs read this run (AGENTS.md gate) — Phase 0 session; every later session re-reads what it uses:
  under node_modules/next/dist/docs/01-app/: 02-guides/upgrading/version-16.md, 01-getting-started/05-server-and-client-components.md, 01-getting-started/12-images.md, 03-api-reference/02-components/image.md, 03-api-reference/05-config/01-next-config-js/images.md, 01-getting-started/13-fonts.md, 03-api-reference/02-components/font.md, 01-getting-started/14-metadata-and-og-images.md, 03-api-reference/03-file-conventions/01-metadata/robots.md, 03-api-reference/03-file-conventions/01-metadata/sitemap.md, 01-getting-started/07-mutating-data.md, 03-api-reference/01-directives/use-server.md, 02-guides/lazy-loading.md, 02-guides/package-bundling.md, 03-api-reference/06-cli/next.md
  1.1 session (2026-09-13): re-read 01-getting-started/13-fonts.md, 03-api-reference/02-components/font.md
  1.2 session (2026-09-13): re-read 01-getting-started/12-images.md, 03-api-reference/02-components/image.md, 03-api-reference/05-config/01-next-config-js/images.md
  1.3a session (2026-09-13): re-read 01-getting-started/05-server-and-client-components.md
  1.3b session (2026-09-13): re-read 01-getting-started/14-metadata-and-og-images.md; read 03-api-reference/04-functions/generate-metadata.md (title, description, metadataBase, URL composition) — Not yet read now: 02-guides/json-ld.md (3.2), 02-guides/forms.md (5.2)
  2.0 session (2026-09-13): re-read 01-getting-started/05-server-and-client-components.md "Context providers"; non-Next sources: @gsap/react 2.1.2 src/types, gsap 3.14.2 exports + gsap-core ticker + ScrollTrigger.update, lenis 1.3.21 README + lenis.mjs + lenis-react.mjs
  Not yet read (as of Phase 0; see per-session lines below): 02-guides/json-ld.md (3.2), 02-guides/forms.md (5.2)
Completed sub-tasks: 0.1, 0.2, 0.3, 0.4, ⛔ Phase 0 gate (plan re-confirmed; D-9 authorship, D-10 visual tooling, D-11 casing check, D-12 gate decisions); 1.1 (owner-approved, italic dropped); 1.2 (owner-approved); 1.3a (owner-approved); 1.3b (owner-approved, D-13 approved); ⛔ Phase 1 gate (owner-approved 2026-09-13); 2.0 (gates green, committed — owner parity review pending)
Next Immediate Action: owner parity review of 2.0 (Parity notes pending user review). On approval: record it, then start 2.1 Hero — read phase-plan 2.1 + cwv-invariants "Hero engine hygiene" + seo-blueprint semantic contract; read Hero.tsx + Hero.module.css fully and write the motion/interaction inventory (scroll states 0–3, pin length, burst-card timing, canvas particle engine, mouse parallax) into STATE before editing; AGENTS.md gate for any Next API touched (server/client split); capture 2.1 before-states (`screenshot.mjs --every 0.25vh` over the hero pin, mobile + desktop, plus reduced-motion) on a production build.
Blockers / Open Questions:
  - DONE in 2.0 (pending owner parity review) → D-14 (owner-approved 2026-09-13): nav section links + back-to-top route through `lenis.scrollTo(target)`; all 10 links + back-to-top now land on target (before: desktop "Process" stopped ~5000 px short, back-to-top run-dependent, mobile About 165 px short).
  - (1.3a, owner call — not blocking) Mobile horizontal overflow: until the CV section has been reached, the mobile page is 466 px wide in a 390 px viewport (`ResumeDashboard … introRight` sits at right=466 — looks like a slide-in that starts off-screen right). Consequence at the top of the page on phones: the fixed nav stretches to 426 px, the hamburger is clipped at the right edge, and when the menu is opened there the close X is essentially off-screen. Confirmed identical before and after 1.3a (pre-existing). Proposal: fix in 2.6 Resume when its motion is ported (clip or re-origin the slide-in) — a visible improvement, so it needs your OK; please also check on a real phone whether the hamburger looks cut off at the top of the page.
  - (1.3a, owner call — not blocking) The footer newsletter form does not send anything: submit shows a 1 s spinner and clears the field (`// Simulate API call`, now in footer/NewsletterForm.tsx, kept verbatim for parity). Zero-placeholder doctrine says this can't ship as-is at the end of the run. Options: (a) remove the newsletter block, (b) wire it in Phase 5 next to the contact action (Resend audience/contacts — would need a DECISIONS entry), (c) keep it as a pure visual (not recommended). Your pick, by Phase 5.
  - (1.3a, owner call — Phase 3 copy sign-off) Template content still in the chrome besides "Nonato": footer Services/Company links point at routes that don't exist (/services/branding, /services/web, /services/app, /services/ai, /about, /careers, /contact; /work arrives in Phase 4), legal links /privacy /terms /cookies 404, footer social links go to the bare twitter.com / linkedin.com / github.com / dribbble.com home pages, mobile-menu socials are href="#", and the nav "▶ Reel" / "Start Project" buttons do nothing. Needs your real links (INTAKE-5 covers social profiles) or removal decisions; the run won't change visible copy without you.
  - 1.1 CLS residual — ACCEPTED by owner 2026-09-13 as recommended; re-judge with PSI numbers in Phase 6 (kept here as the Phase 6 input). Scratch probe, throttled cold load, no input:
      · as shipped on this box (no local Arial → fallback metrics not applied): desktop 0.0004, mobile 0.0008 — one shift when Inter swaps in; sources: nav links row / Start Project button / logo line + the right-aligned hero tagline.
      · simulated Windows/macOS (fallback face served from Arial-metric Liberation Sans): mobile 0.0000; desktop 0.0004 remains (nav links row + tagline — average-width matching can't make every string identical).
      · implication: Android (no Arial; Roboto) behaves like this box ≈ 0.0008 in the field. Lighthouse lab likely loads the three preloaded fonts before first paint (no swap), but that's unverified until Phase 6.
      · Option if Phase 6 isn't 0.000: a new DECISIONS refinement (number assigned then) — an extra Roboto-metric fallback @font-face (local(Roboto), overrides from next's capsize metrics) after next/font's Arial one; and/or JetBrains Mono `preload: false` (not used in the first viewport) to cut 40 KB of early font bytes. (Inter italic already dropped by the owner.) Neither is applied — both would need a DECISIONS entry + approval. Nav is rebuilt in 1.3, which may move the desktop shift sources anyway.
  - Owner call before the Phase 3 copy sign-off (D-12): the template brand "Nonato" is still visible on the page — `src/components/Manifesto/Manifesto.tsx:22` ("At Nonato,"), `:35` (`NONATO`), `src/components/footer/Footer.tsx:158` ("© … Nonato. All rights reserved."). The run does not change visible copy without the owner's instruction.
Audit delta (final, Phase 0 — vs references/audit-baseline.md @ 8828152):
  - WRONG: "no lockfile of any kind exists" — pnpm-lock.yaml (lockfileVersion 9.0) + pnpm-workspace.yaml tracked and in sync → D-7 (audit-baseline.md corrected in place).
  - WRONG: CRLF count — 30 files, not 20, and working-tree-only (git stored LF via core.autocrlf=input; Vercel never received CRLF) → normalized + .gitattributes in 0.3.
  - MISSED: pnpm-workspace.yaml `ignoredBuiltDependencies: [sharp, unrs-resolver]`.
  - MISSED: stray Shop Platform `.env` at repo root → gone by the Phase 0 gate (D-12).
  - MISSED (Next 16): `next build` no longer reports First Load JS; `next/image` `priority` deprecated for `preload`; `images.qualities` defaults to `[75]` → D-8 / relevant to 1.2.
  - MISSED: runtime casing 404 invisible to the import-only `casing` check (ProcessSection.tsx:175) → fixed in 0.3, check extended (D-11).
  - MISSED: dead data `src/data/resumeData.ts:80` → nonexistent `/images/cv/profile.png`, consumer commented out (ResumeDashboard.tsx:256); `casing` warns; left as-is.
  - MISSED: five unused create-next-app SVGs → removed at the gate (D-12).
  - MISSED: visible "Nonato" copy in Manifesto + Footer (see Blockers) — the audit only noted the package name, which is now `personal-portfolio` (D-12).
  - MISSED: body font stack has no metric-compatible fallback — fixed in 1.1 (next/font size-adjusted fallback).
  - CONFIRMED: @react-three/postprocessing, lucide-react, tailwind-merge dead; autoprefixer unreferenced → removed in 0.4. @gsap/react unimported (kept for 2.0).
  - MISSED (Phase 5): `zod` not installed — added with Resend in 5.1 under D-3.
  - OBSERVED: mobile 390×844 at scrollY 844 — hero wordmark "Sina Sotoudeh" overlaps the BUILD burst card. Still present after 1.1, but 1.1 can't decide it: the wordmark uses `--font-display` (not Inter), which renders as DejaVu Sans Bold on this box — much wider than the Arial/Helvetica real phones use. Needs the owner's eyes on a real phone; belongs to 2.1 Hero if real.
  - TOOLING: verify-portfolio.mjs `deps` check was blind (scanned package.json) → fixed (D-8).
Continuity notes:
  - Remote: `origin/main` == local `main` at 110d7f0 when 1.1 started — the owner pushed the Phase 0 commits after the gate (the run itself never pushes). Everything from 1.1 on is local-only until the owner pushes.
  - Commits 3632d32…dfcfa7a (0.1–0.2) carry Claude trailers from before D-9; left as-is (no history rewrite unless the owner asks).
  - .claude/settings.local.json is per-machine — deliberately uncommitted. `.visual/` is gitignored.
  - .gitignore ignores `.env*` — Phase 5.1's `.env.example` needs a `!.env.example` negation.
  - Machine: Playwright 1.63.0 tools at ~/.local/share/portfolio-visual-tools, Chromium 153 system deps installed (D-10). Other servers run on this box (owner's next-servers on :3000 and :3001): stop only your own PIDs. This run used :4310.
  - `pnpm build` needs network for next/font/google (fonts are downloaded at build time); Vercel builds have it.
Last Commit: e60c090 chore(env): phase 1 gate record log hash (previous: 4aa61d7 chore(env): phase 1 gate verification and owner answers)
